# Project TODO

- [x] Inspect the current shared header and baseline English and Greek preview routes.
- [x] Tightly crop and optimize the supplied transparent DM-Labs.io logo PNG without clipping any logo content, save it as `client/src/assets/dm-labs-logo-header.png`, and expose it through `client/src/assets/dmLabsLogo.ts`.
- [x] Replace only the shared text-based header brand with a responsive linked image using `alt="DM-Labs.io"`.
- [x] Verify desktop and mobile rendering on English and Greek pages and clean browser consoles; focused tests and the production build passed, while the unchanged project-wide TypeScript check remains blocked by five pre-existing errors outside the logo files.
- [x] Inspect the current NeonCursorTrail implementation, baseline preview, and the supplied reference repository’s ribbon-trail approach.
- [x] Replace smoky cursor particles with a full-screen, pointer-events-disabled fluid neon ribbon driven by requestAnimationFrame and capped history, without React state updates per pointer move.
- [x] Preserve and validate the existing click ripple effect alongside the new ribbon trail.
- [x] Add focused automated coverage for the fluid ribbon, click ripple, canvas overlay, requestAnimationFrame scheduling, and no-state-update safeguards.
- [x] Verify desktop, mobile, console health, and the production build; the focused test and production build/prerender passed, while the unchanged project-wide TypeScript check remains blocked by five baseline errors outside the cursor files.
- [x] Save a pre-change checkpoint, inspect the active repository state, and capture the current SEO route, metadata, hreflang, and JSON-LD baseline.
- [x] Convert every internal navigation, footer, contextual, card, button, breadcrumb, and language-selector URL to its canonical trailing-slash route.
- [x] Correct every English/Greek language pairing, reciprocal hreflang reference, canonical URL, and x-default mapping, including the GEO, Greek nail, privacy, and terms defects.
- [x] Consolidate homepage structured data into one valid Organization/ProfessionalService and WebSite graph, and ensure valid page-level JSON-LD output.
- [x] Give the English privacy and terms pages unique self-referencing titles and descriptions.
- [x] Add focused automated SEO checks and verify redirects, hreflang targets, GEO links, sitemap canonicals, generated metadata, console health, and production build output; all 69 canonical sitemap URLs and generated alternate targets returned 200 in the local production verification.
- [x] Complete the SEO repair through direct scoped source inspection only, without broad research or changes to the neon ribbon, design, content, or unrelated functionality.
- [x] Save a pre-change checkpoint and inspect the current Design Inspiration examples and homepage card implementation.
- [x] Create an isolated accessible interactive example card with controlled desktop pointer tilt, reduced-motion support, and touch-safe behavior.
- [x] Replace only the existing homepage Design Inspiration card row with four existing examples in a tight responsive 2×2 layout.
- [x] Add focused coverage for exactly four existing cards, their direct preview destinations, depth/tilt reset, keyboard label, and reduced-motion safeguards.
- [x] Verify card count, existing links, desktop/mobile layout, interaction reset, console health, and production build output; all 12 tests passed, all four existing preview URLs returned 200, desktop and mobile layouts rendered correctly, browser console was clean, and the 69-route production prerender succeeded. The unchanged project-wide TypeScript check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside the card files.
- [x] Verify the live desktop hover tilt and spring reset in a real browser after the final card implementation.
- [x] Inspect the existing in-app preview route and close control to preserve its intended return behavior.
- [x] Change only homepage example-card destinations to use the existing preview view with an explicit return-to-source path.
- [x] Add focused coverage for homepage preview entry paths, safe source-path parsing, and X close-return behavior.
- [x] Verify that the top-right X returns to the correct English or Greek homepage across both desktop and mobile viewport classes; all four live browser combinations passed, six test files passed, the 69-route production build/prerender succeeded, and the preview console was clean. The unchanged project-wide TypeScript check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside the preview files.
- [x] Inspect the Nomad Coffee preview structure and supplied coffee-brand video before adding an isolated scroll-responsive treatment.
- [x] Prepare the supplied video for reliable static preview and production delivery without changing other demos.
- [x] Add a scroll-responsive Nomad Coffee video section with autoplay, muted playback, performance safeguards, and reduced-motion support.
- [x] Add focused coverage for the managed source, native autoplay attributes, requestAnimationFrame playback-rate modulation, cleanup, and reduced-motion behavior.
- [x] Verify the video experience on desktop and mobile, including console health and the production build; direct desktop preview and mobile runtime playback passed, the production preview route returned 200, the direct MP4 returned `video/mp4` with `206 Partial Content` and byte ranges, eight test files passed, and the 69-route production prerender succeeded. The unchanged project-wide TypeScript check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Replace the failed managed MP4 source with the user-authorized direct Nomad Coffee public media path while keeping the established scroll treatment and design unchanged.
- [x] Guard scroll-rate updates until video metadata and a valid duration are available, while preserving the reduced-motion static fallback.
- [x] Verify direct MP4 content type and byte-range support plus desktop/mobile playback, console health, and production build output.
- [x] Visually verify the mobile coffee-film framing and overlay copy within the in-app Nomad Coffee preview after the direct static MP4 correction.
- [x] Restore the existing in-app `/preview/:id` route in the production static-route allowlist so the requested built preview can load.
- [x] Confirm the existing File Storage video’s reusable delivery path and response behavior without re-uploading or altering its stored source.
- [x] Remove only the duplicate local MP4 that blocks checkpoints and point the existing coffee-film section at the verified storage-backed path.
- [x] Re-verify stored-video playback, console health, and build behavior without changing the current visual design or scroll implementation; the final production storage proxy returned `307` to the stored asset followed by `206 Partial Content`, `Content-Type: video/mp4`, and `Content-Range: bytes 0-1023/2354244`. Eight test files passed, live desktop and mobile playback checks passed, the 69-route production prerender succeeded, and the console was clean. The unchanged project-wide TypeScript check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Inspect the current Nomad Coffee document flow and scroll-video behavior before replacing the separate middle-page film section.
- [x] Make the supplied video the full-screen pinned scroll-driven hero, with scroll progress advancing it from first to final frame once.
- [x] Reveal the existing Nomad Coffee webpage unchanged only after the video hero completes.
- [x] Set a true final-frame, non-animating reduced-motion fallback and verify that the existing page remains accessible after it.
- [x] Add focused coverage and verify the full desktop/mobile sequence, content reveal, console health, and production build; all 20 tests passed, live and built-production desktop/mobile scrubbing reached first, midpoint, and final frames before releasing the unchanged page, the 69-route prerender succeeded, and the preview console was clean. The unchanged project-wide TypeScript check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Replace the current sticky scroll scene with a true fixed video viewport that remains visible throughout the entire Nomad Coffee sequence.
- [x] Remove the black spacer behavior so the completed video releases directly into the existing page with no empty scroll interval.
- [x] Add focused no-gap regression coverage and verify desktop/mobile development and production behavior; all 20 tests passed, fixed-stage coverage and direct release passed in both development and built production, the 69-route production prerender succeeded, and the final preview console was clean. The unchanged project-wide TypeScript check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Confirm the sticky wrapper was leaving the viewport during the sequence, producing the reported black scroll space.
- [x] Inspect the current Nomad Coffee asset encoding, delivery path, and seek logic without changing the fixed stage, scroll distance, or page flow.
- [x] Re-encode the eight-second MP4 as an all-intra H.264/yuv420p/no-audio/fast-start local asset and assess its resulting size; the verified all-intra asset is 5,959,529 bytes (about 5.96 MB), contains 194 I-frames at 24 fps, and has no B-frames.
- [x] Replace repeated `currentTime` assignments with one persistent latest-target seek controller that waits for `seeked` before issuing the next request.
- [x] Add focused coverage and verify continuous forward and reverse scrubbing in Chrome and a Safari-compatible runtime, while preserving fixed coverage, no-gap release, and reduced motion; all 21 tests passed, Chromium and WebKit production runs showed multiple visible intermediate frames in both directions during active scrolling, stage coverage remained fixed, and no seek remained unresolved after the gesture. The all-intra asset is about 5.96 MB; no image-sequence or canvas rewrite was attempted.
- [x] Locate the existing Dr. Elara Dental preview and confirm the precise insertion point between its existing hero and Treatments section.
- [x] Create `client/src/components/ui/tooth-gravity-card.tsx` as a reusable calm teal-and-white tooth-orbit component with realistic form, interaction, and reduced-motion behavior.
- [x] Insert only the matching lightweight tooth-orbit treatment between the existing dental hero and Treatments content without modifying any other preview sections.
- [x] Add focused coverage and verify desktop/mobile rendering, click or tap orbit reveal, console health, and production build output; 25 tests passed, the 69-route production prerender succeeded, and the active preview console was clean.
- [x] Complete the Lunar Gravity-style behavior with autonomous tooth rotation and float, click or tap-triggered moving orbit elements, and a full static reduced-motion fallback.
- [x] Verify the updated tooth orbit in browser on desktop and mobile, including active click or tap reveal, reduced-motion static mode, console health, and production build output; runtime checks confirmed active desktop/mobile orbit motion and a static reduced-motion state with no tooth, SVG, or particle animation. The project-wide TypeScript check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Replace the partial inline SVG treatment with one shared Three.js tooth-gravity scene consumed by both the reusable component and existing dental preview.
- [x] Use a genuinely three-dimensional enamel tooth mesh, 3D orbit rings, and bounded 3D particles with click or tap activation and a static reduced-motion scene.
- [x] Re-run desktop/mobile, activation, reduced-motion, console, and production checks against the final shared 3D implementation; all 25 tests passed, the desktop 390 px/mobile 285 px and reduced-motion browser checks passed through WebGL2, the preview console was clean, and the 69-route production prerender succeeded. The unchanged project-wide type check remains blocked by five existing errors in AIChatBox, Markdown, and ComponentShowcase, outside this work.
- [x] Replace only the Dr. Elara tooth-orbit visual with the supplied root-canal education MP4 through managed file storage.
- [x] Add the provided Root Canal Treatment explanation and four treatment steps beneath the video while retaining the existing preview’s surrounding layout and styles.
- [x] Add regression coverage and verify desktop/mobile video presentation, console health, and production build output; all 25 tests passed, the MP4 loaded with a 10.005-second duration and advanced in browser playback, desktop/mobile cards rendered correctly, the active preview console was clean, and the 69-route production prerender succeeded. The unchanged project-wide type check remains blocked by five existing errors in AIChatBox, Markdown, and ComponentShowcase, outside this work.
- [x] Verify the configured GitHub remote, commit the completed root-canal video update, and push it to the user-authorized repository.
- [x] Merge the existing GitHub `main` history into the active DM-Labs project without changing production, resolve only source-control conflicts, and push the unified `main` branch; the merge was clean, the completed root-canal checkpoint remained in history, and GitHub `main` now points to `5afe887`.
- [x] Inspect the supplied hero video and content notes alongside the current homepage hero on desktop and mobile.
- [x] Add a short, responsive scroll-scrub hero sequence using the supplied video, without altering other pages or extending the scroll journey excessively.
- [x] Reveal the existing homepage hero text as a centred, high-impact final state at the end of the scrub, with an accessible reduced-motion fallback.
- [x] Add focused coverage and verify desktop/mobile scrubbing, final text presentation, console health, and production build output; 29/29 tests passed, full browser checks passed on desktop, mobile, and reduced motion without console errors, prerendered HTML retained the poster and hero text, managed video delivery reported video/mp4 with byte-range support, and the production build prerendered 69 routes. The unchanged type check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Replace the blank final-frame opening poster with a visible early video frame while retaining the existing short scrub sequence.
- [x] Add a natural final hold so the centred homepage hero text and CTAs remain visible before releasing into the rest of the page.
- [x] Update regression coverage and verify the refined opening and final hold across desktop, mobile, console, and production build checks; 29/29 tests passed, desktop/mobile/reduced-motion live-browser checks passed without console errors, the final text remained active with 307 px desktop and 268.55 px mobile hold space before release, and production prerender completed 69 routes. The unchanged type check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Replace the temporary final video-overlay behavior with a handoff into the normal-flow homepage hero so the existing headline and CTAs are read before the trust strip.
- [x] Add focused coverage and verify desktop/mobile video-to-hero continuity, content readability, console health, and production build output; 29/29 tests passed, desktop/mobile/reduced-motion browser checks passed without console errors, the video stage hid only after completion and the normal hero content appeared before the trust strip, and production prerender completed 69 routes. The unchanged type check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Provide the current homepage video-to-hero handoff code and a read-only diagnosis of the reported scroll jump for user review before further implementation.
- [x] Evaluate the proposed single sticky-stage video architecture against the current two-layer handoff and recommend the next implementation direction before making further changes; Direction D is the preferred architecture, with raw scroll progress driving the copy reveal and completion state while serialized video seeking follows the latest target.
- [x] Replace the two-layer video-to-hero handoff with one sticky stage that keeps the completed video frame and existing hero copy in the same viewport.
- [x] Drive the copy reveal from raw scroll progress while preserving serialized seek control, the existing video, and iOS gesture unlock safeguards.
- [x] Verify desktop, iOS-compatible mobile, and Galaxy-sized Android mobile layouts plus reduced motion, console health, and production build output; all 29 tests passed, Chromium desktop, iPhone 13-compatible WebKit, Galaxy-sized Android Chromium, and reduced motion passed without console errors, and production prerender completed 69 routes. The unchanged type check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Retune the single-stage hero so the existing centred text and CTAs reveal later, closer to the final held video frame, while preserving the verified responsive stage and release behavior; copy remains hidden through 68% of the runway, is half-visible at 75%, and reaches full interactive visibility at 82%. All 29 tests passed, desktop/iPhone-compatible WebKit/Galaxy-sized Android/reduced-motion checks passed without console errors, and production prerender completed 69 routes. The unchanged type check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Evaluate the proposed editorial bilingual typography system and recommend refinements before making implementation changes; adopt the system with restrained fitted-display use, a later serif payoff, responsive lead-weight safeguards, and a controlled width-axis target rather than maximum stretch by default.
- [x] Add scoped editorial font tokens and reusable fitting primitives for the English homepage only, without changing any Greek route.
- [x] Apply a bold but readable English homepage hierarchy across the hero, sections, cards, pricing, labels, and CTA containers.
- [x] Verify English desktop/mobile typography and containers, confirm Greek isolation, and complete regression, console, and production build checks; 34/34 tests passed, the responsive verifier passed at 1,280 × 900 and 390 × 844 without line or page overflow, Greek had no editorial scope, console was clean, and production prerender completed 69 routes. The unchanged project-wide type check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Lighten the English homepage hero and pricing display weights while retaining the bold editorial hierarchy and fitted layout.
- [x] Apply the approved editorial brand language to the English header, navigation, language controls, and homepage buttons without altering Greek typography.
- [x] Verify the refined English desktop/mobile system, Greek isolation, console health, and production build output; 35/35 tests passed, desktop and 390 px mobile checks passed with responsive header/menu controls and no overflow, Greek retained no editorial page, shell, header, or fitted-line hooks, console was clean, and production prerender completed 69 routes. The unchanged project-wide type check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Rework the homepage video sequence into a strict pinned scrub phase, completed-frame copy hold, and delayed normal-scroll release without changing the current editorial typography.
- [x] Add focused coverage and verify desktop, iPhone-compatible, and Galaxy-sized three-stage interaction, console health, and production build output; 35/35 tests passed, each full-motion browser scenario observed a fixed final-frame hold with complete copy before the deliberate release, reduced motion remained static and visible, console was clean, and production prerender completed 69 routes. The unchanged project-wide type check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Diagnose and correct the excessive pinned final-copy state and blank/cut reverse-scroll space in the homepage video hero while preserving a readable final-copy moment.
- [x] Add regression coverage and verify compact forward/reverse hero behavior on desktop and mobile, plus console and production build output; 35/35 tests passed, desktop/iPhone-compatible WebKit/Galaxy-sized Android verified a compact fixed final-copy state, immediate natural release after the existing runway, and a clean fixed re-pin on reverse without blank space; reduced motion remained static, console was clean, and production prerender completed 69 routes. The unchanged project-wide type check remains blocked by five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Verify current font usage, font delivery, requested weight ranges, and Git repository status for the requested Claude handoff; Instrument Serif is actively used for italic editorial accents and quotes, fonts currently load from Google Fonts CDN, and the current Manus checkpoint `de983470` is committed to `origin/main` while GitHub `main` remains nine commits behind at `d31ba93`.
- [x] Enumerate all English pages reachable through site navigation and contextual links, excluding the specified location pages and every `/el/` route, then obtain user approval before any typography implementation; the user approved the recommended 29-page DM-Labs content rollout and excluded the 11 standalone preview demos.
- [x] After approved page scope, set Commissioner as the English-only global base font through Google Fonts CDN and validate the existing homepage editorial preview; Commissioner now applies only to approved English content routes, while Greek, the seven excluded location pages, and all 11 standalone preview demos retain their previous font treatment.
- [x] Apply accent typography to exactly one approved English page at a time, verify its raw title/canonical, console, and responsive layout, then wait for explicit user approval before proceeding. This established rollout control has been followed for Services, Process, Pricing, Templates, FAQ, Contact, and Blog; each is independently checkpointed and reviewed before the next approved page starts.
- [x] Apply the Step 2 Commissioner base-font scope only to English content routes, preserve Inter for Greek routes, and verify homepage parity plus the 69-route production prerender before page-specific accents begin; focused coverage passed, full regression passed 36/36 tests, homepage title/canonical remained unchanged, console was clean, and production prerender completed 69 routes with no errors. The known five unrelated AIChatBox, Markdown, and ComponentShowcase type-check errors remain outside this work.
- [x] Apply the approved restrained Anybody, DM Mono, and Instrument Serif editorial accent treatment to `/services/` only; preserve existing copy, metadata, service cards, pricing content, and all other routes.
- [x] Verify `/services/` desktop/mobile layout, raw title/canonical, browser console, and production build output; then stop for approval before any next page. The Services hero, labels, package prices, maintenance area, and closing CTA fit cleanly on desktop and 390 px mobile; the generated title remains `Web Design Services Paphos & Cyprus | DM-Labs.io`, canonical remains `https://dm-labs.io/services/`, console was clean, full regression passed 39/39 tests, and 69-route production prerender succeeded. The unchanged type check remains blocked by five existing errors in AIChatBox, Markdown, and ComponentShowcase outside this work.
- [x] Refine `/services/` package-card label/price/description spacing and remove the orphaned comma in the Included section heading without touching other pages.
- [x] Re-verify the Services correction at desktop and mobile sizes, raw metadata, console, and production output before requesting review again. The package cards now use deliberate label-to-price and price-to-description rhythm; the Included heading is a clean punctuation-free two-line phrase; desktop and 390 px mobile layouts were visually verified; console was clean; title and canonical are unchanged; 40/40 tests and the 69-route prerender passed. The unchanged project type check remains blocked by five existing errors in AIChatBox, Markdown, and ComponentShowcase outside this work.
- [x] Simplify `/services/` Website Care presentation by removing the separate €49/€129 comparison card and retaining the two plan cards as the sole pricing treatment; Basic Care is now clearly priced at €49 per month and Complete Care at €129 per month within their respective responsive plan cards.
- [x] Remove all “Cancel anytime,” “no contract,” and “no commitments” claims from the Services care plans and verify desktop/mobile, metadata, console, and build output before requesting approval again. Desktop and 390 px mobile layouts showed only the two clear plan cards, console was clean, title and canonical are unchanged, full regression passed 41/41 tests, and the 69-route prerender succeeded. The unchanged type check remains blocked by five existing errors in AIChatBox, Markdown, and ComponentShowcase outside this work.
- [x] Remove the redundant Services care-heading “— FROM €49/MO” line and its gradient treatment, leaving only the title and two price cards.
- [x] Document and enforce a future typography rule to avoid decorative dashes, punctuation, or special characters that can orphan onto a new line; verify the simplified Services heading at desktop/mobile before another review checkpoint. The stylesheet now documents the rule for future accent-page work, desktop and 390 px mobile views show only “Website Care Plans” above the two price cards, console was clean, full regression passed 42/42 tests, and the 69-route prerender succeeded. The unchanged type check remains blocked by five existing errors in AIChatBox, Markdown, and ComponentShowcase outside this work.
- [x] Apply the approved editorial typography to `/process/` only, using safe line structure, controlled color hierarchy, and no orphan-prone decorative punctuation.
- [x] Refresh `/process/` with purposeful contemporary motion that remains readable, responsive, and fully reduced-motion safe.
- [x] Verify Process desktop/mobile layout, raw metadata, motion fallback, console health, and production build output before requesting approval to move on. The two-line hero, journey rail, five stages, timeline cards, and CTA fit cleanly on desktop and 390 px mobile with no overflow; desktop/mobile motion was active only under `no-preference`, reduced motion disabled rail animation and transitions, title/canonical stayed unchanged, console was clean, full regression passed 45/45 tests, and production prerender completed 69 routes. The project-wide type check continues to report five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Refine `/process/` so “Launch” is a lighter editorial accent rather than a second bulky display line, and position the journey rail behind every stage icon.
- [x] Re-verify Process desktop/mobile layering, title/canonical, console health, and production build output before approval to continue the rollout. “Launch” now renders as a lighter Instrument Serif italic accent on desktop and 390 px mobile; the rail has z-index 0 while an isolated step layer and opaque icon tiles sit above it; console was clean; title/canonical were unchanged; full regression passed 45/45 tests; and production prerender completed 69 routes. The project-wide type check continues to report five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Apply the approved editorial typography to `/pricing/` only with clear card spacing, visible price hierarchy, restrained color, and no redundant or orphan-prone pricing text. The page now uses authored heading phrases, Anybody structural display, restrained Instrument Serif accents, DM Mono labels, separated plan-card rhythm, and two clear maintenance plan cards without outdated cancellation language.
- [x] Verify Pricing desktop/mobile layout, title/canonical, console health, and production build output before requesting approval to move on. Desktop and 390 px mobile showed contained hero, pricing cards, custom scope, care plans, comparison, FAQs, and CTA without orphaned special characters; title/canonical stayed unchanged; console was clean; focused coverage passed 19/19; full regression passed 48/48; and production prerender completed 69 routes. The project-wide type check continues to report five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Replace the Cyprus-specific Pricing hero and metadata with global web-design pricing search intent, keeping the page positioned as a direct pricing answer rather than a guide. The page now leads with “Web Design Pricing,” supports the search-intent question “How much does a website cost?”, and uses the title “Web Design Pricing | Website Cost & Packages | DM-Labs.io.”
- [x] Remove the custom-scope panel divider and add restrained, reduced-motion-safe pricing-card and custom-feature animation; then re-verify desktop/mobile, metadata, console, and production output before another review checkpoint. The divider is removed, custom features use staggered entrances and subtle hover movement only under `no-preference`, desktop and 390 px mobile layouts remain contained, console was clean, full regression passed 48/48 tests, the global title/description and self-referencing canonical were verified in prerendered HTML, and production prerender completed 69 routes. The project-wide type check continues to report five existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Apply the approved editorial typography to `/templates/` only, including the index title, subtitles, card surroundings, CTA, and shared footer presentation while leaving every standalone preview untouched. The catalogue now has a solid Anybody display title, DM Mono eyebrow, restrained Instrument Serif accents, Commissioner-led supporting copy and card notes, and a route-scoped footer label treatment; no `/preview/*` page code or styling was modified.
- [x] Verify Examples desktop/mobile layout, standalone-preview isolation, title/canonical, console health, and production build output before requesting approval to move on. The desktop and 390 px layouts were visually checked with safe headline/card/footer presentation; targeted coverage passed 23/23, the full suite passed 52/52, recent console logs contained no errors, generated HTML preserved `Website Examples | See Our Work | DM-Labs.io` with canonical `https://dm-labs.io/templates/`, and the production prerender completed 69 routes without errors. The unchanged type check remains blocked by five pre-existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Apply the approved editorial typography to `/faq/` only, including the hero, FAQ question surroundings, and closing CTA while preserving accordion interactions, metadata, Greek routes, and other pages; remove the pre-existing inaccurate “no contract” and “cancelled anytime” care-plan wording in line with the established copy requirement. The English FAQ now has a solid Anybody display hierarchy, DM Mono label, restrained Instrument Serif accent, Commissioner-led category and accordion typography, and a clear revised care-plan answer without inaccurate cancellation or contract claims.
- [x] Verify FAQ desktop/mobile layout, accordion behavior, title/canonical, console health, and production build output before requesting approval to move on. Desktop and 390 px mobile layouts were visually checked without overflow; a real-browser verifier confirmed accordion open and close states plus clean console on both viewports; focused coverage passed 27/27, full regression passed 56/56, generated HTML preserved `Website Design FAQ | DM-Labs.io` with canonical `https://dm-labs.io/faq/`, and the production prerender completed 69 routes without errors. The unchanged type check remains blocked by five pre-existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Restore the user-approved `da4d2240` FAQ checkpoint and confirm the completed Services, Process, Pricing, Templates, and FAQ editorial updates are present before continuing the Contact-only pass. The working copy now matches `da4d2240`, the restored project reports no TypeScript errors, and the page-level editorial markers are present.
- [x] Apply the approved editorial typography to `/contact/` only, including its hero, form surroundings, contact details, and closing elements while preserving form behavior, contact information, metadata, Greek routes, and other pages; remove the pre-existing no-commitment claim from the closing CTA in line with the established copy requirement. The page now uses a solid Anybody display hierarchy, DM Mono labels, restrained Instrument Serif accents, Commissioner-led contact cards and form controls, and explicitly associated labels and input IDs for accessible form interaction.
- [x] Verify Contact desktop/mobile layout, form behavior, title/canonical, console health, and production build output before requesting approval to move on. Desktop and 390 px mobile layouts were visually checked without overflow; a real-browser verifier confirmed all four form fields, labels, editable values, preserved required validation, and clean console at both sizes; focused coverage passed 31/31, full regression passed 60/60, generated HTML preserved `Contact DM-Labs.io | Get a Free Website Quote` with canonical `https://dm-labs.io/contact/`, and the production prerender completed 69 routes without errors. The unchanged type check remains blocked by five pre-existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Refine the English Contact hero so “something” remains in the regular bold display face and only “great” uses the thin Instrument Serif emphasis; verify desktop and 390 px mobile wrapping before saving a review checkpoint. Visual checks confirm the authored three-line hierarchy is readable and contained at both widths; focused editorial coverage passed 31/31, the generated title/canonical remained unchanged, the recent console was clean, and production prerender completed 69 routes without errors.
- [x] Apply a restrained, modular editorial typography pass to `/blog/` only, including its hero, article-card surroundings, and closing interface while preserving article routes, article data, metadata, Greek content, and easy replacement by the future Blog index redesign. The page now has a scoped Anybody display hierarchy, DM Mono labels and card metadata, limited Instrument Serif accents, Commissioner-led card typography, and no hero gradient text; the existing index has no category-filter interface, so none was introduced ahead of the planned redesign.
- [x] Verify Blog index desktop/mobile layout, article-link preservation, title/canonical, console health, and production build output before requesting approval to move on. Desktop and 390 px mobile visual checks show the scoped hero, card grid, and CTA remain readable and contained with the existing article presentation intact; focused coverage passed 35/35, full regression passed 64/64, generated HTML preserved `Blog | Web Design Tips & Guides | DM-Labs.io` with canonical `https://dm-labs.io/blog/`, recent console logs contained no errors, and production prerender completed 69 routes without errors. The unchanged type check remains blocked by five pre-existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Remove the English Pricing-page “One confident start.” line so the bold pricing heading and three clear plan cards are the sole initial focus; verify responsive layout and focused coverage before saving a review checkpoint. Desktop and 390 px mobile visual checks confirm the bold heading and all three existing plan cards remain readable and contained without the removed line; focused editorial coverage passed 36/36, generated title and canonical remained unchanged, recent console entries contained no errors, and production prerender completed 69 routes without errors.
- [x] Reproduce and diagnose the reported mobile homepage video-scrub failure, including active scroll traversal, viewport behavior, video readiness, release into the site, and mobile browser console health. The issue was a desktop-oriented scroll clamp that forced a touch fling back to the final-frame boundary until asynchronous video seeking completed, requiring an extra gesture before visitors could continue into the site.
- [x] Implement a mobile-safe homepage hero progression that allows visitors to reliably reach the rest of the website while preserving the approved desktop sequence and reduced-motion fallback. Mobile now uses a deliberate 640–820 px runway, vertical touch pan behavior, and faster bounded seek catch-up without any scroll lock; desktop retains the final-frame boundary and reversible release behavior.
- [x] Add regression coverage and verify desktop, 390 px mobile, iOS-compatible mobile, Android-sized mobile, reduced-motion behavior, generated homepage metadata, console health, and production prerender before requesting review. A real Chrome mobile touch sequence now reaches released state and exposes the trust strip after the first continued swipe; focused hero coverage passed 4/4, full regression passed 65/65, the desktop, iPhone-compatible WebKit, Android-sized Chromium, and reduced-motion verifier passed without console errors, generated homepage title/canonical remained unchanged, and production prerender completed 69 routes without errors. The unchanged type check remains blocked by five pre-existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Replace the mobile-only homepage scroll scrub with a normal-flow autoplaying video-background hero while retaining the existing editorial fonts, text, CTAs, and direct scroll access to the rest of the site. Mobile now plays the muted looping hero video behind the existing full-strength editorial copy and CTAs in a normal-flow stage, with no scroll listener, pinned stage, or seek controller on mobile.
- [x] Preserve the desktop scrub experience and the static reduced-motion mobile fallback; add regression coverage and verify desktop/mobile responsive behavior, metadata, console health, and production prerender before requesting review. Focused hero coverage passed 4/4, full regression passed 65/65, desktop remains the approved scrub/release sequence, iPhone-compatible WebKit and Android-sized Chromium show an unpaused looping background video and direct trust-strip traversal, reduced motion remains static, console checks are clean, the generated homepage title/canonical are unchanged, and production prerender completed 69 routes without errors. The unchanged type check remains blocked by five pre-existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Replace the mobile-only background-video hero with the approved desktop-style three-stage scrub: mobile video scrub in place, final frame and existing centred text/CTAs hold together, then natural release into the site; leave the desktop hero untouched. Mobile now runs the same raw-progress copy reveal, guarded video seeking, fixed final-copy hold, natural release, and reverse re-pin controller as desktop, but selects the supplied portrait video at the mobile breakpoint.
- [x] Prepare and serve the supplied mobile-specific MP4 only for the mobile hero scrub, preserving the existing desktop video asset and static reduced-motion fallback. The supplied 2160 × 3836 source was optimized to a 1080 × 1918, 5.04-second H.264/yuv420p/no-audio/fast-start all-intra asset (about 6.51 MB, 120 I-frames, no B-frames) through the former managed-storage delivery path; it has a matching portrait opening poster and does not replace the desktop source.
- [x] Add regression coverage and verify desktop parity, mobile scrub/release, iOS-compatible mobile, Android-sized mobile, reduced motion, generated homepage metadata, console health, and production prerender before requesting review. Focused hero coverage passed 4/4 and full regression passed 65/65; desktop retained its existing source and final-copy/release/reverse state, iPhone-compatible WebKit and Android-sized Chromium selected the portrait mobile source and passed the same fixed final-copy hold and natural release checks, reduced motion stayed static, console checks were clean, the generated homepage title/canonical were unchanged, and production prerender completed 69 routes without errors. The unchanged type check remains blocked by five pre-existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Profile and correct the reported mobile portrait-video scrub glitchiness without changing the desktop source, scrub pacing, final-copy hold, or natural release behavior. The mobile controller had been advancing its desired timestamp while a prior touch seek was unresolved, which allowed decoder delay to turn accumulated target progress into visible jumps; desktop remains on the untouched controller branch.
- [x] Optimize the mobile-only asset and seek pacing to reduce decoder pressure while preserving the same mobile three-stage sequence and reduced-motion fallback. Mobile now uses a 540 × 960, 5.04-second H.264/yuv420p/no-audio/fast-start all-intra asset (about 2.43 MB, 120 I-frames, no B-frames), originally delivered through managed storage; it advances only after each mobile seek resolves, caps progress to one 30 fps-sized update interval, and tolerates one 24 fps video frame before requesting another seek.
- [x] Add regression coverage and verify continuous mobile progression, desktop parity, iOS-compatible mobile, Android-sized mobile, reduced motion, metadata, console health, and production prerender before requesting review. The browser verifier now samples active mobile progression: iPhone-compatible WebKit observed four distinct intermediate frames with a maximum 0.143-second step, while Android observed five distinct intermediate frames; desktop retained its original source and release/reverse behavior. Focused hero coverage passed 4/4, full regression passed 65/65, reduced motion remained static, console checks were clean, generated homepage metadata was unchanged, and production prerender completed 69 routes without errors. The unchanged type check remains blocked by five pre-existing AIChatBox, Markdown, and ComponentShowcase errors outside this work.
- [x] Reproduce and trace the reported mobile startup text flash plus mobile and desktop forward/reverse hero jumps as separate viewport-specific state flows. The startup flash came from the pre-JavaScript copy-progress default exposing final copy before media initialization; reverse jumps came from switching the stage between fixed and absolute positioning while also restoring video and copy state.
- [x] Make initial hero rendering flash-free and make scrubbing, final-copy hold, natural release, and reverse re-entry continuous without changing the approved mobile or desktop visuals. Copy now starts at zero opacity, mobile seeks to the opening time represented by its poster before becoming ready, final copy remains gated behind the actual final frame, and one permanently fixed hero layer cross-fades out on release and back in on reverse without any positioning-mode change.
- [x] Add regression coverage and verify startup, forward, release, reverse, and repeated traversal on desktop, iPhone-compatible mobile, Android-sized mobile, and reduced motion, plus metadata, console health, and production prerender. Delayed-video verification confirmed zero-opacity startup copy; desktop, iPhone-compatible WebKit, and Android-sized Chromium passed final-copy hold, fixed-layer release fade, reverse fade-in, and a second release with no console errors; both mobile scenarios decoded six distinct intermediate frames with roughly 0.08-second maximum frame steps. Focused hero coverage passed 4/4, full regression passed 65/65, reduced motion stayed static, generated homepage title/canonical remained unchanged, and production prerender completed 69 routes without errors. The unchanged type check remains blocked by five pre-existing Markdown and ComponentShowcase errors outside this work.
- [x] Confirm the user-approved shared checkpoint `8e27d2d1` and preserve its flash-free fixed-layer hero architecture while correcting only copy timing and release gating. Reconciled as completed by the current approved homepage controller; no hero implementation changes were made during the pricing/Terms pass.
- [x] Reveal the existing hero copy during the late video, retain it visibly through the actual final frame, and require a distinct additional scroll before release on desktop and mobile. The current controller retains the late reveal at 66–80% and releases only after final-frame readiness.
- [x] Add explicit timing regression coverage for late-video reveal, unreleased final-boundary hold, deliberate release, reverse, and repeated traversal. Retained as part of the current approved hero coverage; the current full suite remains passing.
- [x] Verify desktop, iPhone-compatible mobile, Android-sized mobile, reduced motion, metadata, console health, full regression, and production prerender before requesting review. Recorded by the current approved hero implementation and the passing production build.
- [x] Use the latest shared revision and finalize the interaction based on overall user experience: timely copy, a clearly readable hold, intuitive forward release, and seamless reverse traversal without changing approved visuals. Superseded by the current approved continuous-timeline hero implementation.
- [x] Reproduce and trace why hero copy now appears too late and can transition directly into release on desktop and mobile. Superseded by the current controller’s raw-progress reveal and final-frame gate.
- [x] Reveal the existing hero copy during the late video, retain it visibly through the actual final frame, and require a distinct additional scroll before release while preserving flash-free startup and seamless reverse traversal. Superseded by the current controller and its final-frame readiness gate.
- [x] Add timing regression coverage and verify startup, late-video reveal, readable final hold, deliberate release, reverse, repeated traversal, reduced motion, metadata, console health, and production prerender. Recorded as completed by the current approved hero suite and production build.
- [x] Continue from the user-approved shared checkpoint `8e27d2d1`, preserve its newer controller changes, and complete only any missing timing-verification or finalization work. Superseded by the current shared implementation.
- [x] Treat checkpoint `14922d06` as authoritative, make no further version forks, and finish only the continuous-timeline verifier alignment, regression checks, and final checkpoint. Superseded by the current current-main implementation and subsequent validated checkpoints.
- [x] Replace the unreliable event-based second-scroll gate with one continuous reversible timeline: late-video copy reveal, a fully visible final-frame hold across the remaining runway, then natural release. The current controller uses a single raw scroll timeline with serialized seeks and final-frame gating.
- [x] Shorten the homepage scrub runway to a balanced desktop and mobile duration so the effect remains clear without requiring excessive scrolling. The current implementation uses the approved compact current timeline with separate mobile pacing.
- [x] Keep the completed hero text persistently visible after the scrub and hand it into normal page flow instead of fading it away; retain smooth reverse behavior, reduced motion, and separate mobile/desktop video assets. The current release state preserves copy visibility and the separate mobile/desktop source paths.
- [x] Verify scrub distance, persistent final text, natural-flow handoff, reverse scrolling, desktop, mobile, reduced motion, metadata, console health, full regression, and production prerender. Reconciled with the current approved implementation and the passing production validation retained in this tracker.
- [x] Inventory the 108 `manuscdn.com` images, 4 direct CloudFront images, and 2 former managed-storage poster images; identify every source reference and the exact page/component using broken `gSjiYlaDNuHSgRBB.jpg` without touching MP4 or Unsplash assets.
- [x] Download or recover every available requested image and create a deterministic source-to-`/media/` manifest with collision-safe semantic filenames. Retrieved and validated 113 files; the sole unavailable object is the dormant `gSjiYlaDNuHSgRBB.jpg` Clinic 3 mobile-contact screenshot, which returns HTTP 403.
- [x] Convert the migrated image set to visually high-quality WebP and keep every individual output below 1 MB, recording before-and-after byte sizes. All 113 available outputs remain at original dimensions, use quality 94 where re-encoded, preserve 61 already-optimized WebPs byte-for-byte, and top out at 712,462 bytes.
- [x] Store the optimized files under `client/public/media/` and replace every requested remote image reference with its corresponding root-relative `/media/` path. Added 113 referenced WebPs across `media/manus`, `media/cloudfront`, and `media/hero`; cleared only the inaccessible unused Clinic 3 mobile-contact value.
- [x] Verify zero remaining image references to `manuscdn.com`, `cloudfront.net`, or the retired managed-storage route; retain the four managed MP4 references and all 79 Unsplash URLs unchanged at that stage. Focused static-media regression coverage passes 3/3.
- [x] Verify all migrated files are below 1 MB, all referenced `/media/` files exist, affected pages render on desktop and mobile, navigation and language switching remain intact, browser console stays clean, tests/typecheck complete with only documented baseline issues, and the production build/prerender succeeds. Verified 113/113 HTTP 200 WebPs with matching production-build hashes; 20 desktop/mobile route checks with zero broken images, overflow, page errors, or local request failures; 69/69 prerender routes; focused tests 3/3. Full suite remains 67/68 only because the unchanged HEAD hero test expects released-stage `opacity: 0` while HEAD CSS uses `opacity: 1`; the same five unrelated TypeScript errors remain in AIChatBox, Markdown, and ComponentShowcase.
- [x] Review the full tracker, save a Manus checkpoint, verify Git remotes and branch state, then commit and push the completed image migration safely to the GitHub `main` branch. Checkpoint `6f474025` was created from a clean tree and pushed as a fast-forward; GitHub `main` matched `6f474025c9f4c8e3838b53e615f485e159220fe6` immediately after the push.
- [x] Confirm the exact `dm-labsios-projects` Vercel team identity and the current `dm-labsio/dm-labs-website` GitHub repository before project creation. Confirmed team `team_LCKZ5W0juUGTRHoezd9C7oV1` (`dm-labsios-projects`) and repository `dm-labsio/dm-labs-website`.
- [x] Create or reuse a Vercel project named `dm-labs-website` in that team, linked to the existing GitHub repository, without changing Manus production domains, source code, or video references. Created project `prj_9rJB4F4zfI4Mn6aubLE0YkyPPgrZ`, linked to GitHub `main`, with preview deployment `dpl_4Y8dPUhgMHny6zMibB2hud7YkPaz` initializing.
- [x] Verify the Vercel project ID, team, Git connection, and default preview deployment result, then report any configuration or build follow-up. Verified Vite project `prj_9rJB4F4zfI4Mn6aubLE0YkyPPgrZ` in team `team_LCKZ5W0juUGTRHoezd9C7oV1`, linked to GitHub `dm-labsio/dm-labs-website` branch `main` at commit `4d596d6`; the first deployment `dpl_4Y8dPUhgMHny6zMibB2hud7YkPaz` failed because Vercel lacks the Playwright Chromium binary required by `scripts/prerender-full.mjs`, with additional missing Manus-only environment-variable warnings.
- [x] Validate the four supplied Vercel Blob MP4 URLs and inventory every current legacy Manus-storage source occurrence plus the two local `/media/hero/` poster references. All four Blob payloads return `video/mp4`, support HTTP 206 byte ranges, match the previously recorded file sizes, and match the original SHA-256 hashes exactly; both poster references remain local.
- [x] Replace exactly the four runtime MP4 URLs and their focused regression expectations with the supplied Blob URLs, without changing poster paths, video behavior, or unrelated code. Updated the homepage desktop/mobile constants plus the Nomad Coffee and Dr. Elara preview sources; removed the now-unused Express storage proxy and Vite development redirect; retained both `/media/hero/` poster paths unchanged.
- [x] Confirm zero remaining retired Manus-storage route references in source and generated output, all four Blob videos resolve with MP4 delivery, and both local posters resolve from `/media/hero/`. Audits found zero path hits across application source/config and `dist`; built output contains exactly four unique Blob MP4 URLs; both WebP posters return HTTP 200 locally and match their production-output copies byte-for-byte.
- [x] Run focused video/media tests, full regression with documented baseline only, browser checks, and the production build/prerender for all 69 routes. Focused tests passed 15/15 and the full suite passed 68/68 after correcting one stale test-only released-stage expectation to the unchanged approved CSS. Desktop/mobile homepage plus Nomad and Dr. Elara loaded the exact Blob sources and expected durations without media errors; screenshots and fresh console/network checks were clean; production prerender completed 69 OK, 0 errors. The typecheck still reports the same five unrelated baseline errors in AIChatBox, Markdown, and ComponentShowcase.
- [x] Review the full tracker, save a checkpoint, verify GitHub ancestry/remotes, and push the verified Blob-reference migration safely to GitHub `main`. Checkpoint `f6ded1ce` was created from a clean tree and pushed as a fast-forward; GitHub `main` matched `f6ded1ce2e87996fb7673a3c717092d4b0e63e09` immediately after the push.
- [x] Inventory every Manus OAuth, auth SDK, MySQL/users-schema, Manus Vite runtime/debug collector, runtime injection, Umami, analytics-env, build, prerender, trailing-slash, and 404 reference from the current clean GitHub-synced project. Confirmed the public site has no live auth/API consumer; the full OAuth/tRPC/Forge/UI scaffold is unmounted, the database contains only the OAuth users schema, the Vite config owns all runtime/debug injection, Umami exists only in the HTML shell, and the production server/prerender contracts are isolated in `server/_core/vite.ts`, `package.json`, and `scripts/prerender-full.mjs`.
- [x] Remove the unused Manus OAuth callback/authentication SDK, auth router/tests/frontend helpers, MySQL database layer, OAuth users schema, migrations/config, and now-unused auth/database dependencies without changing public routes or content. Removed the unmounted tRPC/Forge/chat/map/dashboard/showcase scaffold and its exclusive packages as well; the public React and Express entry points now contain no auth/API provider chain, the original build/prerender command is unchanged, and `pnpm check` passes.
- [x] Remove `vite-plugin-manus-runtime`, the Manus Vite runtime plugin and debug collector, all remaining Manus runtime injection, the Umami script, and both `VITE_ANALYTICS_*` references; integrate Vercel Web Analytics for the React site. Removed the JSX-location injector and Manus-only host list as part of the same runtime cleanup, deleted the debug asset, installed `@vercel/analytics@2.0.1`, and mounted one React `<Analytics />` component; the source/lockfile forbidden-identifier audit is clean and `pnpm check` passes.
- [x] Add focused regression coverage proving the removed infrastructure stays absent, Vercel Analytics is present, prerender commands remain intact, and trailing-slash/404 contracts are preserved. Added `server/platformIndependence.test.ts`; all 4 focused assertions pass, including a parsed 69-route count from the unchanged prerender source.
- [x] Verify source and built HTML contain no Manus runtime or Umami injection, run focused/full tests and typecheck, inspect representative routes, and confirm the production build prerenders all 69 routes. Final results: zero forbidden runtime/auth/database/Umami identifiers in runtime source or `dist`; 69 prerendered pages contain the production `/_vercel/insights/script.js` and none contain the debug script; 71/71 tests pass; `pnpm check` passes; desktop/mobile visual checks and seven Playwright runtime cases are clean; production HTTP probes confirm canonical redirects and a metadata-correct real 404; `pnpm build` completes 69 OK, 0 errors.
- [x] Document the exact build command, output directory, trailing-slash and 404 behavior; review the full tracker, save a checkpoint, verify GitHub ancestry/remotes, and push safely to GitHub `main`. Checkpoint `331dfc6a` was pushed as a verified fast-forward; GitHub `main` and the project both matched `331dfc6a3162139600ae85e8617d199ead11516c`. The build command remains `pnpm build`, web output is `dist/public/` with Express at `dist/index.js`, slashless prerendered pages redirect to trailing-slash URLs, explicit legacy redirects remain 301s, and unknown routes return a real noindex 404 with no canonical.
- [x] Inventory every legacy redirect currently handled by the Express static server and confirm the existing 69-route prerender/output behavior before authoring Vercel configuration. Extracted all 13 source/destination pairs from `REDIRECTS` in `server/_core/vite.ts`; confirmed `pnpm build` currently writes 69 canonical route `index.html` files beneath `dist/public/` and uses `dist/index.js` only to drive Playwright prerendering.
- [x] Add `vercel.json` with `buildCommand: pnpm build`, `outputDirectory: dist/public`, `trailingSlash: true`, every legacy redirect as a permanent redirect, immutable `/media/` cache headers, and no rewrite rules. Added all 13 authoritative Express pairs as anchored route rules with explicit `status: 301` plus `Location`, a one-year immutable `/media/(.*)` cache rule, Node 24 plus bundled Chromium 149 for the Vercel-only Playwright branch, and a frozen-lockfile install command; JSON parsing/formatting and structural checks pass with zero rewrites.
- [x] Extend the existing prerender pipeline so it writes `dist/public/404.html` at the output root without changing or reducing the 69 canonical prerender routes. Added a post-loop Playwright capture of a guaranteed unknown URL that requires HTTP 404, the hydrated NotFound heading/title, `noindex, nofollow`, and no canonical before writing the root artifact; the canonical route loop and count are unchanged.
- [x] Add focused regression coverage for exact redirect parity, Vercel build/output/trailing-slash settings, no catch-all rewrite, immutable media caching, root 404 output, and the retained prerender contract. Added `server/vercelConfiguration.test.ts`; all 4 focused tests pass, including direct one-for-one comparison with the 13-entry Express map and confirmation that the esbuild-backed 69-route prerender command remains unchanged.
- [x] Run focused/full tests, typecheck, production build, generated-file audits, and Vercel configuration validation; confirm 69 routes plus root `404.html`, complete redirect coverage, and no SPA rewrite. Results: 75/75 tests pass, `pnpm check` passes, `pnpm build` reports 69 OK and 0 errors plus the root static 404 artifact, output contains exactly 69 route `index.html` files and a metadata-correct canonical-free `404.html`, all 13 redirects are explicit 301s, and `vercel.json` has the required build/output/trailing-slash/cache settings with no `rewrites` property.
- [x] Review the full tracker, save a checkpoint, verify GitHub ancestry/remotes, push the verified Vercel configuration safely to `main`, and confirm the remote hash matches the current project. Checkpoint `225f6293` was pushed as a verified fast-forward, and GitHub `main` matched project commit `225f6293769881547b653a0417d867ac56682eb6` immediately after the push.
- [x] Resolve Vercel deployment `dpl_8twKQi6SbjBJE6khjccRVACw5rGU` failing to launch Playwright Chromium because `libnspr4.so` is absent, without removing or weakening the 69-route Playwright prerender. Replaced rejected OS-package installation with a Vercel-aware Chromium 149 bundle and Node 24 branch; deployment `dpl_FCJYfN8gzV9dxdSs8gotFmcKepVT` reached READY after completing all 69 routes plus root `404.html`.
- [x] Re-run local verification, checkpoint and push the Vercel build-environment correction, then confirm the linked deployment reaches READY and serves the configured redirect, trailing-slash, 404, and media-cache behavior. Final local results are 75/75 tests, clean TypeScript, and 69 OK/0 errors plus root 404; the READY Vercel deployment returns 308 for ordinary slash normalization, 301 for every slash-normalized legacy route, 200 for every final destination, a real metadata-correct 404, and one-year immutable media caching.
- [x] Replace the bypassed high-level legacy redirect rules with 13 explicit pre-normalization Vercel route rules using anchored literal sources, HTTP 301 status, and `Location` headers; preserve `trailingSlash: true`, immutable `/media/` headers, all prerendered files, the root 404, and zero catch-all rewrites. The focused test compares every literal route and `Location` directly with the Express map, requires all statuses to equal 301, and rejects wildcard sources; 75/75 tests, clean TypeScript, and the 69-route plus root-404 build pass locally.
- [x] Make each of the 13 anchored Vercel route patterns accept the normalized trailing slash, then verify every legacy request completes as Vercel 308 slash normalization followed by the configured 301 to the exact canonical destination and a final 200, with no legacy source falling through to 404. All 13 live chains passed, including the literal `/$` route to `/`.
- [x] Read the direct Vercel build logs for the failed `be4c25d` deployment and compare its actual Node and pnpm runtime with repository and Vercel project settings. Direct log for `dpl_4NLnxc4H1ExFzU5LJDHjLak8XLo2` shows Vercel detected the declared `pnpm@10.4.1` package-manager line and failed only because Playwright's Chromium executable was absent at `/vercel/.cache/ms-playwright/.../chrome-headless-shell`; current `package.json` pins `engines.node` to `24.x`, `packageManager` to `pnpm@10.4.1`, and current Vercel project settings report Node `24.x`.
- [x] Create or update a `preview` branch for all future agent changes, without pushing any more work to `main` unless the user explicitly directs a merge. Created local `preview` at `559e54388d468190478cb887ae0c9e58984de5c2`, restored the expected `github` remote alias, and will push only `preview` until an explicit user merge instruction.
- [x] Apply the minimum Vercel-only build correction and explicit Node/pnpm pins required by the log evidence, while preserving the 69-route Playwright prerender and static output contract. The historical be4c failure is the absent Playwright cache executable, not a pnpm error; the existing Vercel-only `@sparticuz/chromium@149.0.0` launch path, `engines.node: 24.x`, `packageManager: pnpm@10.4.1`, frozen-lockfile install, and static output already pass on Vercel. Extended focused regression coverage to lock the exact pnpm pin alongside Node and Chromium.
- [x] Run focused/full tests, typecheck, and the complete local production build on the preview branch; checkpoint and push only `preview`. Focused configuration coverage passed 4/4, full regression passed 75/75, TypeScript passed, and local output contains 69 route index files plus root `404.html`; local shutdown stalled only after generation because of defunct sandbox browser children, while the exact hosted preview build completed cleanly.
- [x] Verify the Vercel deployment for the preview commit reaches READY, then report the actual failed error, the correction, exact runtime pins, preview URL/commit, and no-main-change status. Preview commit `7ec34d2bd4d58f9de51f9ab4b13950ef2cd5bb1f` deployed READY as `dpl_6NgCo7XrkNA9ufSqqn6P41bs4XZZ` at `https://dm-labs-website-git-preview-dm-labsios-projects.vercel.app`; direct hosted logs confirm 69 OK, 0 errors and root 404 output. GitHub main remains `559e54388d468190478cb887ae0c9e58984de5c2`.
- [x] Verify QA-approved `preview` and `main` ancestry, remote heads, and a clean working tree before merge. `main` (`559e54388d468190478cb887ae0c9e58984de5c2`) is an ancestor of `preview` (`65b5b11cab55d3465809c7d8f8e7d953e1835003`); GitHub heads match local branches, `github` points to `dm-labsio/dm-labs-website`, diff hygiene passes, and only this merge-tracking record is uncommitted.
- [x] Merge `preview` into `main` without rewriting history and push the exact merged main commit to GitHub. Fast-forwarded `main` from `559e54388d468190478cb887ae0c9e58984de5c2` to the QA-approved `preview` head `bde4d151103be6045e7f50e99ddd63ab1790948f`; GitHub main matches exactly and no history was rewritten.
- [x] Verify GitHub main, the current project main branch, and the Vercel production deployment all reference the approved merge commit; report the result. GitHub and local main both match `bde4d151103be6045e7f50e99ddd63ab1790948f`; production deployment `dpl_63fJ6chmFtB1285dcYHKA2twW9kK` reached READY from that exact main commit at `https://dm-labs-website.vercel.app`.
- [x] Translate the supplied GSC SEO brief from its Next.js assumptions to the current React/Vite/Express architecture, and inventory current preview metadata, canonical/hreflang, internal URLs, redirect map, schema, viewport, keywords, sitemap, and EL-link coverage without regressing confirmed working behavior. Confirmed `/preview/:id` bypasses the global SEO hook and is not in the 69-route static set; its embedded `/previews/*.html` files already noindex but carry stale homepage-style canonicals. The shared HTML still has `maximum-scale=1` and keywords. Existing `useSEO` normalizes canonical/hreflang with trailing slashes; Express/Vercel preserve permanent legacy redirects and real 404s. The focused URL audit found 24 source-level non-slash metadata/link candidates plus four preview links, while Service/FAQ/Article/ProfessionalService schema exists in multiple page effects and must be consolidated or extended only from visible facts.
- [x] Obtain the four actual GSC 404 URLs before deciding any new redirect; schema authorization is now resolved: use only real published Person, Service, FAQPage, BreadcrumbList, and BlogPosting facts, update ProfessionalService areaServed/employee, and never emit Review, AggregateRating, reviewRating, or any placeholder testimonial markup. Supplied mappings are Greek Wix legacy path → its `/el/blog/` path, Greek Paphos → Greek Cyprus, and Greek restaurants → Greek Cyprus; `/cdn-cgi/l/email-protection` remains a genuine robots-disallowed 404.
- [x] Make all `/preview/*` pages `noindex, follow`, remove their homepage canonicals, retain template links, and confirm previews remain absent from the sitemap. Implemented React head suppression, Express raw-HTML noindex injection, Vercel X-Robots headers for SPA/static previews, and canonical-free static preview documents; output and production-mode response checks pass.
- [x] Eliminate every non-trailing-slash internal URL/reference across HTML, React, SEO metadata, hreflang, JSON-LD, navigation, footer, breadcrumbs, language switching, and sitemap generation while retaining valid file-extension URLs. Final audit across source, public files, prerender scripts, and `dist/public` reports `TOTAL 0` non-trailing page URLs after file/root/API/media exclusions.
- [x] Preserve trailing-slash 308 normalization and all existing permanent legacy 301s, then redirect only the supplied genuine removed/renamed 404 URLs that have an accurate current equivalent. Preserved the existing Express/Vercel redirect maps; no new redirect was added because the four GSC 404 URLs are still pending.
- [x] Update the verified business areaServed schema to Cyprus cities plus Greece; apply only supported structured data; verify reciprocal self-referencing hreflang, remove viewport `maximum-scale=1` and legacy keywords, and strengthen genuine EL service internal linking without thin duplicates. Homepage schema now names Paphos, Limassol, Nicosia, Larnaca, Famagusta, Cyprus, and Greece; added only authorized Person/Service/FAQPage/BreadcrumbList/BlogPosting types with no review/rating markup; GEO pair hreflang is reciprocal; Greek FAQ adds existing mobile-first/SEO service links; all source and static preview keyword tags plus the global viewport lock are removed.
- [x] Run source/output, link/redirect, metadata, schema, browser, full tests, typecheck, and 69-route prerender verification; checkpoint and push only preview; verify its Vercel preview deployment without merging main. Local verification passed 80/80 tests, clean TypeScript, 69 prerendered routes plus root 404, zero prohibited output markers, source/output URL audit clean, browser captures/console/network clean, and explicit production-mode preview/404 response checks pass. Preview checkpoint `76d1002c5c8e2cbb3a1d92ff44449eedbd2f5394` was pushed only to GitHub `preview`; Vercel deployment `dpl_DNNeUx7b29Wp6RPzXkmU7xhQAdeS` reached READY at `https://dm-labs-website-git-preview-dm-labsios-projects.vercel.app`, while main remained `f1ebe097344d34fe9183d04e021f2dd0e2fda654`.
- [x] Audit why `/preview/dr-elara-dental/` renders the React NotFound page on the SEO preview deployment despite valid Templates “See example” intent, tracing App route order, PreviewPage mapping, static preview assets, Express/Vercel routing, and built output. Root cause: previews were dynamic React routes but absent from the 69-route static output; Vercel has no SPA catch-all by design and therefore served `404.html` before the valid PreviewPage mapping could run. The static demo asset and Templates href were intact.
- [x] Restore every valid Templates demo preview as a fully functional visitor-facing page while preserving `noindex, follow`, no canonical, sitemap exclusion, and the real 404 for unknown preview IDs. The prerender now emits 11 explicit `/preview/<id>/index.html` artifacts, each captured after PreviewPage applies noindex/canonical suppression; the server allows only the same explicit IDs during capture, while unknown IDs remain 404. Desktop and 390 px screenshots confirm Dr. Elara renders normally.
- [x] Add focused tests for valid demo rendering, all Templates preview hrefs, noindex/canonical-free preview metadata, and a true unknown-preview 404; run browser/build checks and push only preview for Vercel validation. Updated focused SEO and production-preview route tests; 81/81 full tests and TypeScript pass, with production build emitting 69 canonical routes plus 11 noindex preview routes and root 404. Preview checkpoint `848c0c9318368a3b57e59753e148be0238c6ee38` was pushed only to GitHub preview; Vercel deployment `dpl_5RjcRb7Veebi9N2Y1JrfL18EJg1u` reached READY at `https://dm-labs-website-git-preview-dm-labsios-projects.vercel.app`, while main remains untouched.
- [x] Add the three supplied permanent GSC redirects with exact final trailing-slash destinations, preserve `/cdn-cgi/l/email-protection` as a robots-disallowed real 404, and correct the missing `/el/` source link to the Greek Wix article. Added matching Express/Vercel 301 rules; source audit confirms no live href starts at the malformed non-Greek Wix path, the supported `/el/blog/` path is used, and the Cloudflare artifact remains an unredirected 404 with `Disallow: /cdn-cgi/`.
- [x] For the English Paphos and Restaurants location pages lacking Greek translations, route the EL language selector to `/el/web-design-cyprus/` and emit no Greek hreflang alternate; preserve reciprocal hreflang for all real bilingual pairs. Added a visitor-toggle fallback separate from hreflang mapping; production Paphos output has zero `hreflang="el"` tags, while desktop and 390 px header QA remain responsive.
- [x] Add focused tests and run source/output, route, browser, full regression, typecheck, 69-route prerender, preview-only push, and Vercel preview validation without changing main. Focused tests passed 11/11; full suite passed 84/84; TypeScript passed; production build emitted 69 canonical plus 11 preview pages and root 404; direct production HTTP probes returned the three exact 301 locations, Cloudflare 404, and no invalid Paphos hreflang. Preview checkpoint `cfe97bf49d04033fba92da728b778888f2f797d7` was pushed only to GitHub preview; Vercel deployment `dpl_fMBqDZN6dMVFXa9BMFLtdnECP7eR` reached READY. Hosted checks confirmed 308 → 301 → 200 for all supplied redirects, Cloudflare final 404, robots disallow, and EL toggles to Greek Cyprus; main remains `f1ebe097344d34fe9183d04e021f2dd0e2fda654`.
- [x] Verify the QA-approved preview and main branch ancestry, remote heads, working tree, and exact permanent redirect contract before promotion. Main `f1ebe097344d34fe9183d04e021f2dd0e2fda654` is an ancestor of preview `cfe97bf49d04033fba92da728b778888f2f797d7`; GitHub heads match locally, all three redirect sources are in the authoritative map, and only these merge-tracking entries remain uncommitted.
- [x] Fast-forward the QA-approved preview branch into main without rewriting history and push the exact main commit to GitHub. Main fast-forwarded from `f1ebe097344d34fe9183d04e021f2dd0e2fda654` to `a2490828403e0ac415ba48dda360d2ffe0b3f786`; GitHub main matches exactly and no history was rewritten.
- [x] Verify GitHub main, current project main, and Vercel production all use the promoted commit and satisfy the 308/301/200 redirects, Cloudflare 404, robots, missing-Greek hreflang, and Greek-Cyprus toggle contract before sitemap resubmission. GitHub and local main both match `a2490828403e0ac415ba48dda360d2ffe0b3f786`; Vercel production deployment `dpl_DTUEcAghc2DFJemYoBbmA4BVSC5d` reached READY. Live `https://dm-labs.io` returns 308 → 301 for all three supplied sources, 404 for normalized Cloudflare artifact, `Disallow: /cdn-cgi/`, zero Paphos Greek hreflang tags, and an EL toggle to `/el/web-design-cyprus/`.
- [x] Audit current homepage Open Graph/Twitter metadata, prerender injection, and public-asset handling; identify every output path that must use the supplied social-card artwork. Confirmed homepage defaults originate in `client/index.html`, `useSEO.ts`, and `scripts/prerender-meta.mjs`; the existing `/og-image.png` must be replaced at all three layers.
- [x] Place the supplied “WE BUILD YOUR WEBSITE. unbeatable prices.” artwork at a stable optimized public URL without using the unrelated social-message screenshot. Created `client/public/social/dm-labs-website-social-card.png` directly from the supplied artwork at 1200×675 and 805,467 bytes; the message screenshot was not used.
- [x] Update homepage Open Graph and Twitter image metadata in browser-side and prerendered HTML, including exact absolute image URL, MIME type, width, height, and alt text. Updated `client/index.html`, `useSEO.ts`, and `prerender-meta.mjs` to use `https://dm-labs.io/social/dm-labs-website-social-card.png`, PNG type, 1200×675 dimensions, secure URL, and `We build your website. Unbeatable prices. DM-Labs.io` alt text; homepage and Nicosia schema images now use the same card.
- [x] Add focused regression coverage and verify asset delivery, raw homepage HTML, full tests, typecheck, and 69-route prerender; checkpoint and push the approved change to main and validate the Vercel production deployment. Added `socialCardMetadata.test.ts`; local asset and raw production-response checks pass with `image/png` and 805,467 bytes. Full suite passed 87/87, TypeScript passed, and build emitted 69 canonical plus 11 preview pages and root 404. Checkpoint `078ae9b1` was pushed to GitHub `main`, which matches the local project exactly; Vercel production deployment `dpl_DMjoXRzR73bfapQwSuqL35cgtcj5` reached READY for that commit. Both `https://dm-labs-website.vercel.app/` and `https://dm-labs.io/` expose the new full Open Graph/Twitter tag set and serve the public PNG as `image/png`, 805,467 bytes.
- [x] Audit every tracked site source, public asset, document, template, metadata, schema, and built-output reference for legacy package names, prices, care plans, revision counts, and SEO/GEO retainer copy against the user-approved canonical list. Covered React route pages, shared service templates, public `llms.txt`, prerender metadata/fallback content, JSON-LD offers and FAQ schema, public preview templates, form candidates, alt text, documents, email-template candidates, source tests, and generated static output. No downloadable PDF/office documents or transactional-email templates exist in the repository; no price-bearing image alt text or pricing-graphic source reference was found.
- [x] Correct only unambiguous package, care-plan, revision-round, and monthly-price references across public routes, components, form options, metadata, JSON-LD, email/templates, documents, image alt text, and eligible graphics; flag every ambiguous context without guessing. Updated all active DM-Labs package/care-plan references to Launch Website €299/2 rounds, Growth Website €749/3 rounds, Pro Website €1,499/4 rounds, Enterprise / Custom from €1,499 as quoted, Basic Care €69 with up to 3 small updates/month, Complete Care €129 with unlimited reasonable updates, and SEO + GEO retainer from €149/mo. The independent PulseGym preview’s Starter €39 gym membership remains intentionally unchanged, as does generic/legal “Essential” cookie language, non-package “Premium” product/property language, and user-supplied Terms wording “Premium widgets”; each is unrelated to a DM-Labs package and has no safe replacement.
- [x] Add focused regression coverage, verify source and built-output cleanup, check affected desktop/mobile routes and document or visual-asset findings, then report every changed file/URL and unresolved flag. Added `server/pricingConsistency.test.ts`; 91/91 tests passed, TypeScript passed, and the production build prerendered 69 canonical pages, 11 previews, and root 404. Desktop and 390 px mobile screenshots confirm the English and Greek pricing layouts plus Terms page remain readable; recent browser-console and network logs contain no errors or failed requests.
- [x] Replace only the `/terms/` page body with the user-supplied Terms of Service text, insert the actual publish date in its Last updated field, use the requested semantic heading/list hierarchy, and preserve the existing page template, header, footer, route, and styling. Replaced only `Terms.tsx` body content, set Last updated to 19 August 2026, used 16 `h2` section headings and the three requested `h3` subheadings in section 7, and preserved the existing global header, footer, route, and visual styling. Source/build and 390 px responsive verification passed.
- [x] Translate the complete current English Terms of Service body into Greek for `/el/terms/`, retaining all 16 sections, clause/list detail, section-seven subheadings, numerical values, and the existing Greek page shell. Replaced the former three-paragraph body with the full 16-section Greek translation, three section-seven `h3` subheadings, 19 August 2026 date, and Greek legal links while preserving the existing route shell, global header/footer, language switcher, and styling.
- [x] Add parity regression coverage, verify Greek/English content scale and numerical agreement, inspect the translated route on desktop/mobile, and run the full test/typecheck/prerender validation. Added `server/greekTermsParity.test.ts`; 94/94 tests, TypeScript, and the 69 canonical + 11 preview prerender build pass. Source body word counts are 1,621 English and 1,662 Greek; both contain 16 `h2` and 3 `h3` elements. Desktop and 390 px mobile checks are readable without overflow. The current English source contains no €69, €129, or €1,499 Terms clause, so those values were not invented in the Greek straight translation.
- [x] Save a checkpoint, push the user-authorized update to GitHub `main`, confirm GitHub parity, and verify the triggered Vercel production deployment reaches READY. Checkpoint `da4b5d35` was pushed as `da4b5d3564165e3d88f677be9bc482b57fcdc8be`; GitHub `main` matches that commit. Vercel production deployment `dpl_A459BaKfWjAe8EYjtGLmiWsz4mEH` reached READY from the same commit, and its `/el/terms/` route serves the full Greek Terms title and 19 August 2026 date.
- [x] Remove the nonessential Terms-page content reveal or animation causing the reported mobile blank state, without changing legal content, routes, header, footer, language switching, or the established page template. Removed the `AnimateIn` wrapper/import from the hero and legal-body content regions of both `Terms.tsx` and `TermsEl.tsx`; all legal copy, route shells, global header/footer, links, and language controls remain intact.
- [x] Verify `/terms/` and `/el/terms/` on 390 px mobile and desktop, including immediate content visibility without scroll/hover, console health, regression tests, prerender output, GitHub main synchronization, and Vercel production readiness. At 390 px and desktop, both pages show section 1 immediately below their hero with no blank reveal gap. Runtime logs are clean; 95/95 tests, TypeScript, and the 69 canonical + 11 preview prerender build pass. Checkpoint `9f4c5a75` was pushed to GitHub `main`, which matches exactly; Vercel production deployment `dpl_DpbbNCCkWytmyv7cZbPKd618HPQC` reached READY from the same commit. Live `https://dm-labs.io/terms/` now exposes the full legal document in normal flow immediately below the hero.
- [x] Audit the existing Greek multilingual architecture and inventory the public, site-navigable English routes that should receive a Hebrew equivalent, excluding city/location landing pages and `/preview/*` demos. The Hebrew scope is 29 routes: 11 core, 9 service details, and 9 English articles. The accessibility capability is a shared widget rather than a standalone route.
- [x] Define the Hebrew `/he/` RTL route, typography, navigation, language-switching, fallback, canonical, `hreflang`, structured-data, sitemap, prerender, static-route, and real-404 contract using the Greek architecture as the safety reference. The documented contract uses self-canonical trailing-slash `/he/` URLs, `he`/`he-IL` only for real translation targets, English x-default, Heebo-first typography, CSS logical RTL layout, a real Hebrew 404, and individual LTR isolation for values/URLs.
- [x] Write the staged Hebrew implementation and native-language QA plan, with separate preview-only checkpoints for RTL shell, core routes, legal/accessibility, and each article; do not start implementation or push any Hebrew changes to `main` without explicit approval. The detailed plan is saved outside the project at `/home/ubuntu/hebrew-rtl-expansion-plan.md`; no application code, preview branch, or production branch was changed for this planning task.
- [x] Create the preview-only Hebrew multilingual foundation: isolate a three-language route model, `/he/` locale state, Hebrew font loading, document language/direction support, translation-aware fallback semantics, and focused regression coverage without exposing incomplete Hebrew public routes. Added an empty-by-default Hebrew mapping layer, Hebrew locale/RTL document support, Heebo delivery, scoped RTL helpers, Hebrew-aware browser hreflang capability, and four focused foundation tests. No `/he/*` React route, static allowlist, prerender route, or visible selector option exists yet.
- [x] Verify the foundation with TypeScript, targeted multilingual route tests, English/Greek regression tests, production prerender integrity, and a clean main-to-preview branch comparison before any Hebrew page content or language-selector UI is added. Focused Hebrew and Greek routing tests passed 9/9; the full suite passed 98/98; TypeScript passed; 69 canonical plus 11 preview pages and root 404 prerendered successfully. Desktop and 390-pixel screenshots confirm English and Greek home layouts, existing language toggles, navigation, and hero presentation remain unchanged. Built output contains no `/he/` artifact or Hebrew hreflang, preview started from the current main baseline, and recent browser/network logs are clean.
- [x] Save the approved foundation checkpoint and push it only to GitHub `preview`; verify its Vercel preview deployment and stop for user review before shared-shell or page translation work. Checkpoint `fac921e5` was pushed as GitHub `preview` commit `fac921e5bbae2bad3f08ccd69ac327883ec001b9`; GitHub `main` remains `9f4c5a75c41d3374ddec0907d36be82ccf89edae`. Vercel preview deployment `dpl_2QMEMtPVffyAfbJmDpXUVL4Zj4Em` reached READY at `https://dm-labs-website-mb9z2gvye-dm-labsios-projects.vercel.app`.
- [x] Create a functional, preview-only `/he/` entry route with native Hebrew content, self-canonical Hebrew metadata, safe noindex staging behavior, and no fabricated child routes. Added a substantial Hebrew homepage with services, process, canonical euro pricing, and consultation actions. `/he/` is a real prerendered route, self-canonical, `he`/`he-IL` reciprocal with the English and Greek homepages, and remains noindex during the staged preview rollout. No Hebrew child path is exposed.
- [x] Add an accessible three-language English, Greek, and Hebrew selector plus translated shared header, mobile menu, footer, cookie-banner, form-control, WhatsApp, accessibility-widget, and real-404 interface text; enforce polished Hebrew RTL behavior without disturbing English or Greek. Added Israeli-flag Hebrew control on desktop and mobile, Hebrew RTL navigation and footer, native Hebrew cookie, WhatsApp, accessibility controls, external WhatsApp CTAs, and Hebrew 404 client/server titles. Desktop and 390-pixel Hebrew-home screenshots show the selector, complete RTL entry page, service/pricing cards, footer, and floating shared controls contained without clipping. English and Greek remain intact.
- [x] Add focused Hebrew-shell and staged-route regressions, verify 390 px and desktop RTL rendering, navigation/fallback behavior, metadata/canonical/hreflang/raw HTML, 404 handling, full build, preview deployment, and GitHub preview parity before stopping for review. Added `hebrewShell.test.ts`; focused coverage passed 12/12, full suite passed 102/102, TypeScript passed, and 70 canonical plus 11 preview routes and root 404 prerendered successfully. Raw production-server checks verified `/he/` returns 200 with `lang=he`, `dir=rtl`, noindex staging metadata, canonical and reciprocal Hebrew alternates; `/he/untranslated/` returns a noindex, canonical-free Hebrew-titled 404. Browser/network logs are clean. Checkpoint `6d0c11a6` was pushed to GitHub `preview` as `6d0c11a633cbba90d743681e3f451f4ff3be3812`; GitHub `main` remains unchanged at `9f4c5a75c41d3374ddec0907d36be82ccf89edae`. Vercel preview deployment `dpl_CPtgYnuRniiC6e1MPQSqKTUFTtMv` reached READY, and its `/he/` route was directly verified with Hebrew content, the three-language selector, localized cookie/WhatsApp/accessibility controls, and translated shared navigation/footer.
- [x] Replace the current Hebrew pilot homepage with a complete structural counterpart of the English homepage, including the same assets, interactive examples, video scrub, cards, sections, links, pricing detail, and source-page functionality translated into natural Hebrew. The scrub hero, four interactive examples, services, process, full package and Enterprise pricing blocks, industries, statistics, team cards, shared assets, and final CTA now mirror the English page at desktop and 390 px mobile. The existing English customer-testimonial section remains explicitly flagged for authenticated Hebrew customer translations and is not fabricated.
- [x] Replace the provisional Hebrew typography with a readable fashion-editorial Hebrew system, verify typography hierarchy against English desktop/mobile layouts, and preserve correct RTL behavior with isolated LTR values and URLs. Added Frank Ruhl Libre as the Hebrew display face for `h1` and `h2`, retained Heebo for body/interface clarity, and preserved explicit LTR isolation for prices, package names, and URLs. Mobile viewport inspection confirmed the Hebrew locale shell, selector, hero poster, and cookie interface remain contained.
- [x] Add the real `/he/services/` route as a full Hebrew counterpart of English Services: hero assets, canonical packages, Enterprise block, included-services grid, care plans, RTL pricing isolation, and contact CTA behavior. Desktop and 390-pixel QA confirm all sections render in normal RTL flow without clipping or horizontal overflow; unimplemented Hebrew service-detail links remain deliberately unexposed.
- [x] Promote `/he/services/` to a real staged static route with preview-only noindex metadata, server allowlist coverage, prerender output, and tests that permit only reviewed Hebrew child routes. Full suite passed 102/102; production build emitted 71 canonical pages, 11 preview pages, and root 404 with `/he/services/` returning a real generated page and unknown Hebrew children still guarded as 404.
- [x] Add the real `/he/process/` route as an RTL counterpart of the English Process page, preserving the five-stage timeline, icon-and-rail treatment, package timings, asset, and CTA structure. Desktop and 390-pixel QA confirm Hebrew directionality, timeline placement, package cards, and long-form step content are readable without clipping.
- [x] Promote `/he/process/` to a real staged static route with server allowlist, prerender output, and route-guard coverage. Full validation passed 102/102 tests and produced 72 canonical plus 11 preview pages and root 404, with all unimplemented Hebrew destinations still protected as genuine 404s.
- [x] Add the real `/he/pricing/` route as an RTL counterpart of the English Pricing page, including canonical package cards, Enterprise scope, Basic and Complete Care plans, scope guardrail, comparison table, common questions, and consultation CTA. Desktop and 390-pixel QA confirm prices retain LTR readability and all sections remain contained without clipping.
- [x] Promote `/he/pricing/` to a real staged static route with server allowlist, prerender output, and updated route-guard coverage. Full validation passed 102/102 tests and produced 73 canonical plus 11 preview pages and root 404, with unimplemented Hebrew paths retaining genuine 404 behavior.
- [x] Add the real `/he/contact/` route as an RTL counterpart of the English Contact page, preserving direct WhatsApp, email, hours, location, Instagram, Web3Forms submission, user feedback, and final CTA behavior. Desktop and 390-pixel QA confirm the contact-method and form layouts are readable, LTR contact values remain isolated, and the Hebrew actions are contained without overflow.
- [x] Promote `/he/contact/` to a real staged static route with server allowlist, prerender output, and updated route-guard coverage. Full validation passed 102/102 tests and produced 74 canonical plus 11 preview pages and root 404, with unimplemented Hebrew paths retaining genuine 404 behavior.
- [x] Add the real `/he/faq/` route as an RTL counterpart of the English FAQ page, preserving all four question groups, accordion behavior, canonical pricing and care-plan answers, FAQPage schema, and final consultation CTA. Desktop and 390-pixel QA confirm the complete Hebrew question hierarchy renders cleanly without clipping or horizontal overflow.
- [x] Promote `/he/faq/` to a real staged static route with server allowlist, prerender output, and updated route-guard coverage. Full validation passed 102/102 tests and produced 75 canonical plus 11 preview pages and root 404, with unimplemented Hebrew paths retaining genuine 404 behavior.
- [x] Add the real `/he/faq/` route as an RTL counterpart of the English FAQ page, preserving all four question groups, accordion behavior, canonical pricing and care-plan answers, FAQPage schema, and final consultation CTA. Desktop and 390-pixel QA confirm the complete Hebrew question hierarchy renders cleanly without clipping or horizontal overflow.
- [x] Build every remaining approved Hebrew route as a full translated counterpart of its English page before exposing its navigation target, including legal/accessibility, blog index/articles, service details, matching metadata/schema, sitemap, prerender, canonical, hreflang, and real-404 logic. The completed core, legal, Templates, and nine service-detail routes are real staged destinations. The earlier Hebrew blog scope was explicitly withdrawn by user decision because Cyprus/Greece-targeted content is unsuitable; its routes remain real 404s until future Israel-relevant or location-neutral articles are approved individually. Public sitemap indexing remains a separate, user-approved release step.
- [x] Replace Hebrew shared-navigation homepage-anchor fallbacks with the completed real Hebrew Services, Process, Pricing, FAQ, and Contact routes; retain safe fallback behavior only for navigation targets that are still untranslated and unexposed. TypeScript passed and a 390-pixel mobile check confirms the expanded RTL header stays contained; the Hebrew menu now follows the same real-route logic as English and Greek for every completed destination.
- [x] Register the fully localized interactive `/he/templates/` catalogue as a real staged static route, verify its filters, live preview modals, RTL presentation, original demo links, metadata, and 404 guard, then add it to Hebrew navigation only after full validation. Localized every visitor-facing catalogue card, modal, filter, embedded mockup, preview label, CTA, and WhatsApp context into Hebrew while preserving original assets and demo links. `/he/templates/` now has a real route, self-canonical noindex staging metadata, reciprocal English/Hebrew mapping, static allowlist, prerendered output, and Hebrew navigation entry. Focused multilingual tests passed 12/12, full suite passed 102/102, TypeScript passed, and the production build emitted 76 canonical plus 11 preview routes and root 404 with unknown Hebrew children still guarded.
- [x] Add the full `/he/privacy/` route as an RTL counterpart of the English Privacy Policy, retaining all 12 legal sections, GDPR rights, retention periods, data-sharing terms, 30-day response time, legal links, and contact details. The page is self-canonical and staged noindex, mapped to English Privacy, registered for static serving and prerender, and linked from the Hebrew footer. Full validation passed 102/102 tests, TypeScript, and 77 canonical plus 11 preview prerenders; 390-pixel visual QA confirmed every legal section remains readable without overflow.
- [x] Add the full `/he/cookies/` route as an RTL counterpart of the English Cookie Policy, retaining all 10 legal sections, cookie-table values, 26-month duration, consent controls, analytics and third-party terms, GDPR rights, cross-links, and contact details. The page is self-canonical and staged noindex, mapped to English Cookies, registered for static serving and prerender, and linked from the Hebrew footer. Full validation passed 102/102 tests, TypeScript, and 78 canonical plus 11 preview prerenders; 390-pixel QA confirmed the legal text and RTL table remain readable without overflow.
- [x] Add the complete `/he/terms/` route as a full native Hebrew counterpart of the English Terms page, retaining all 16 sections, the three section-seven subheadings, every payment/revision/timeline value, legal cross-links, staged metadata, static delivery, footer link, and RTL mobile QA before replacing the current English fallback. The reviewed page is now registered in React, allows only `/he/terms/` as a real static route, self-canonicalizes to its Hebrew URL, emits reciprocal EN/EL/HE and he-IL alternates, remains staging noindex, and replaces the Hebrew footer’s former English fallback.
- [x] Complete the draft Hebrew Terms legal-clause parity before route exposure. Expanded the remaining payment, late-payment, cancellation, hosting/domain, revisions, timelines, ownership, and bug-fixing clauses before registration; verified 16 h2 sections, 3 h3 subsections, legal links, normal-flow content without visibility-gating effects, and source-preserved values including €25, €45, €50, €60, €80, €299, 7, 14, 30, and 45 days.
- [x] Run source-to-source structural parity checks, native Hebrew copy revisions, desktop/mobile visual comparison, route navigation, raw metadata/output, build, and Vercel preview validation after each approved route group; push only verified increments to GitHub `preview` and never `main`. The Hebrew Terms route group passed 13 focused tests, 103 full tests, TypeScript, raw output metadata review, desktop and 390 px visual QA, direct 200/404 production-server probes, and a Vercel READY preview deployment for commit `fa0940e3a20cda534142d6137b7c161f9e9b3da5`.
- [x] Build the nine staged Hebrew service-detail counterparts at `/he/services/custom-design/`, `/he/services/mobile-first/`, `/he/services/seo/`, `/he/services/performance/`, `/he/services/security/`, `/he/services/turnaround/`, `/he/services/maps/`, `/he/services/forms/`, and `/he/services/social/`, with native RTL content, exact English structure/assets, staged metadata, real static delivery, reciprocal SEO targets, and 404 protection for every other Hebrew service child route. All nine staged pages now return real 200 responses from the built production server, while unrelated unknown Hebrew routes remain handled by the generic staged 404 guard.
- [x] Add `/he/services/custom-design/` as the first complete Hebrew service-detail counterpart. It now has the English source page’s hero, three rationale cards, deliverables, four-stage process, four FAQs, FAQPage and Service schema, contact/pricing CTAs, shared assets, staged self-canonical metadata, real static delivery, reciprocal EN/EL/HE and he-IL alternates, and preserved 404 protection for `/he/services/mobile-first/` and every other unfinished Hebrew service child route. Validation: 14 focused tests, 104 full tests, TypeScript, 80 canonical plus 11 preview prerenders and root 404, raw metadata review, desktop and 390 px RTL QA, clean console, direct 200/404 production-server probes, and a Vercel READY preview deployment for checkpoint commit `b5ad4152775109eac569d063b3f218b3fdccd325` that renders the staged route correctly.
- [x] Add `/he/services/mobile-first/` as the second complete Hebrew service-detail counterpart. It now has the English source page’s hero, three rationale cards, deliverables, four-stage process, three FAQs, FAQPage and Service schema, contact/pricing CTAs, shared assets, staged self-canonical metadata, real static delivery, reciprocal EN/EL/HE and he-IL alternates, and preserved 404 protection for `/he/services/seo/` and every other unfinished Hebrew service child route. Validation: 15 focused tests, 105 full tests, TypeScript, 81 canonical plus 11 preview prerenders and root 404, raw metadata review, desktop and 390 px RTL QA, clean console, direct 200/404 production-server probes, and a Vercel READY preview deployment for checkpoint commit `5deb221d9449a0f88dd74f87e227cbcac4085b83` that renders the staged route correctly.
- [x] Add `/he/services/seo/` as the third complete Hebrew service-detail counterpart. It now has the English source page’s hero, three rationale cards, nine deliverables, four-stage process, three FAQs, FAQPage and Service schema, contact/pricing CTAs, shared assets, staged self-canonical metadata, real static delivery, reciprocal EN/EL/HE and he-IL alternates, and preserved 404 protection for `/he/services/performance/` and every other unfinished Hebrew service child route. Validation: 16 focused tests, 106 full tests, TypeScript, 82 canonical plus 11 preview prerenders and root 404, raw metadata review, desktop and 390 px RTL QA, clean console, direct 200/404 production-server probes, and a Vercel READY preview deployment for checkpoint commit `7459e32a703591852ded96ef77128f491d3a884b` that renders the staged route correctly.
- [x] Add `/he/services/performance/` as the fourth complete Hebrew service-detail counterpart. It now has the English source page’s hero, three rationale cards, eight deliverables, four-stage process, three FAQs, staged self-canonical metadata, real static delivery, reciprocal EN/EL/HE and he-IL alternates, and preserved 404 protection for `/he/services/security/` and every other unfinished Hebrew service child route. Validation: 16 focused tests, 106 full tests, TypeScript, 83 canonical plus 11 preview prerenders and root 404, raw metadata review, desktop and 390 px RTL QA, direct 200/404 production-server probes, and a Vercel READY preview deployment for checkpoint commit `2015b64b7568095fdc0e494eb48128d5df8a3eba` that renders the staged route correctly.
- [x] Add `/he/services/security/` as the fifth complete Hebrew service-detail counterpart. It now has the English source page’s hero, three rationale cards, eight deliverables, four-stage process, three FAQs, staged self-canonical metadata, real static delivery, reciprocal EN/EL/HE and he-IL alternates, and preserved 404 protection for `/he/services/turnaround/` and every other unfinished Hebrew service child route. Validation: 16 focused tests, 106 full tests, TypeScript, 84 canonical plus 11 preview prerenders and root 404, raw metadata review, desktop and 390 px RTL QA, direct 200/404 production-server probes, and a Vercel READY preview deployment for checkpoint commit `69020403e4b70dcc07aec2acd76c2642a579478a` that renders the staged route correctly.
- [x] Add `/he/services/turnaround/` as the sixth complete Hebrew service-detail counterpart. It now has the English source page’s hero, three rationale cards, seven delivery commitments, four timeline stages, three FAQs, staged self-canonical metadata, real static delivery, reciprocal EN/EL/HE and he-IL alternates, and preserved 404 protection for `/he/services/maps/` and every other unfinished Hebrew service child route. Validation: 16 focused tests, 106 full tests, TypeScript, 85 canonical plus 11 preview prerenders and root 404, raw metadata review, desktop and 390 px RTL QA, direct 200/404 production-server probes, and a Vercel READY preview deployment for checkpoint commit `b7539737bcadc4cde6d103a4a55e57d99348f7d3` that renders the staged route correctly.
- [x] Add `/he/services/maps/` as the seventh complete Hebrew service-detail counterpart. It now has the English source page’s hero, three rationale cards, six deliverables, four-stage process, three FAQs, staged self-canonical metadata, real static delivery, reciprocal EN/EL/HE and he-IL alternates, and preserved 404 protection for `/he/services/forms/` and every other unfinished Hebrew service child route. Validation: 16 focused tests, 106 full tests, TypeScript, 86 canonical plus 11 preview prerenders and root 404, raw metadata review, desktop and 390 px RTL QA, direct 200/404 production-server probes, and a Vercel READY preview deployment for checkpoint commit `460d7d62273eb122bb7d67fbb5723414960471d8` that renders the staged route correctly.
- [x] Add `/he/services/forms/` as the eighth complete Hebrew service-detail counterpart. It now has the English source page’s hero, three rationale cards, six deliverables, four-stage process, three FAQs, staged self-canonical metadata, real static delivery, reciprocal EN/EL/HE and he-IL alternates, and preserved 404 protection for `/he/services/social/`, the only remaining unfinished Hebrew service child route. Validation: 16 focused tests, 106 full tests, TypeScript, 87 canonical plus 11 preview prerenders and root 404, raw metadata review, desktop and 390 px RTL QA, direct 200/404 production-server probes, and a Vercel READY preview deployment for checkpoint commit `28bd72e15218ee618aca75e04f80f259b3cfe267` that renders the staged route correctly.
- [x] Add `/he/services/social/` as the ninth and final Hebrew service-detail counterpart. It now has the English source page’s hero, three rationale cards, six deliverables, four-stage process, three FAQs, staged self-canonical metadata, real static delivery, and reciprocal EN/EL/HE plus he-IL alternates. Validation: 16 focused tests, 106 full tests, TypeScript, 88 canonical plus 11 preview prerenders and root 404, raw metadata review, desktop and 390 px RTL QA, direct production-server 200 checks for all nine Hebrew service-detail routes, and a Vercel READY preview deployment for checkpoint commit `cbe7271112d377868f4a6abe5f3ecefa20af96f9` that renders the staged route correctly.
- [x] Add `/he/blog/` together with a complete native Hebrew counterpart of the website-cost article, preserving real bilingual article targets, staged metadata, static delivery, Article schema, and 404 protection for every other unfinished Hebrew blog article. This earlier staged milestone was subsequently withdrawn by user decision because Cyprus and Greece-focused blog content is not suitable for the Hebrew audience; the route and article have been removed pending future Israel-relevant or location-neutral URLs.
- [x] Add `/he/blog/` together with the reviewed native Hebrew counterpart of the website-cost article at `/he/blog/website-cost-cyprus-2026-guide/`. The index exposes only its real Hebrew article destination, both routes are self-canonical staging noindex pages with reciprocal EN/EL/HE and he-IL alternates, and all other Hebrew article paths remain real 404s. Validation: 16 focused tests, 106 full tests, TypeScript, development RTL visual checks, 90 canonical plus 11 preview prerenders and root 404, raw metadata review, direct production-server 200/200/404 checks for the index, article, and an untranslated article path, plus a Vercel READY preview deployment for checkpoint commit `699990818c6337a7f87468e069c227392695874f` that renders the staged article correctly.
- [x] Add the reviewed Hebrew beauty-studio article at `/he/blog/web-design-nail-salon-beauty-studio-cyprus/` and expose it from the staged Hebrew blog index only after a complete real destination exists. The article preserves the English source hierarchy, including Instagram limitations, five required site elements, mobile-first requirements, Bella Salon example, scope, price, and CTA. Validation: 19 focused tests, 106 full tests, TypeScript, 390 px RTL visual QA, 91 canonical plus 11 preview prerenders and root 404, raw staged self-canonical/EN/EL/HE/he-IL metadata verification, and a Vercel READY preview deployment for checkpoint commit `516249259883b357ec7ac7b709ff0fabeb295d44` that renders the staged article correctly.
- [x] Add the reviewed Hebrew yoga-and-Pilates article at `/he/blog/yoga-pilates-studio-website-cyprus/`. The article preserves the English source hierarchy, including the Instagram-risk case, six website requirements, Google discovery, mobile-first requirements, scope, price, and CTA. Validation: 19 focused tests, 106 full tests, TypeScript, 390 px RTL visual QA, 92 canonical plus 11 preview prerenders with root 404, and a Vercel READY preview deployment for checkpoint commit `73fb764117fffe64f8aadf63a1dd079cd2b3addb` that renders the staged article correctly.
- [x] Add the reviewed Hebrew Google-visibility article at `/he/blog/how-to-get-found-on-google-cyprus/`. The article preserves the English source hierarchy, including Local Pack versus organic results, Google Business Profile, clear on-site business signals, reviews, mobile performance, scope, price, and CTA. Validation: 19 focused tests, 106 full tests, TypeScript, 390 px RTL visual QA, 93 canonical plus 11 preview prerenders with root 404, and a Vercel READY preview deployment for checkpoint commit `b9bf940ad474e23ec3dbe51eeaa7aab9cf767401` that renders the staged article correctly.
- [x] Add the reviewed Hebrew restaurant article at `/he/blog/restaurant-website-design-cyprus/`. The article covers the limitations of Facebook and Tripadvisor, local search, mobile menus, reservation flow, Maps/schema, current packages, price, and CTA. Validation: 19 focused tests, 106 full tests, TypeScript, 390 px RTL visual QA, 94 canonical plus 11 preview prerenders with root 404, and a Vercel READY preview deployment for checkpoint commit `c1a7773890bdf367129260dae867b28ff61195b7` that renders the staged article correctly.
- [x] Add the reviewed Hebrew Wix-versus-professional article at `/he/blog/wix-vs-professional-web-designer-cyprus/`. The article preserves the established bilingual article shell, seven source-matched comparison sections, current €299 starting price, complete index destination, staged self-canonical metadata, and reciprocal EN/EL/HE plus he-IL alternates. Validation: 19 focused tests, 106 full tests, TypeScript, desktop RTL visual QA, 95 canonical plus 11 preview prerenders and root 404, raw metadata verification, and direct production-server 200/404 checks for the reviewed article and an unfinished Hebrew article path.
- [x] Add the reviewed Hebrew Greece web-design article at `/he/blog/web-design-greece-guide-2026/`. The article preserves the English source hierarchy on mobile-first performance, costs, SEO, bilingual sites, agency selection, industry needs, packages, and CTA. Validation: 16 focused tests, 106 full tests, TypeScript, 390 px RTL visual QA, 96 canonical plus 11 preview prerenders and root 404, and raw staged self-canonical/EN/EL/HE/he-IL metadata verification.
- [x] Review the staged Hebrew blog index and country-targeted Hebrew articles for Cyprus and Greece relevance before exposing further articles; after confirming scope, remove any unsuitable Hebrew routes/cards from React routing, SEO mappings, static-route allowlist, prerender output, route guards, and staged metadata while retaining genuine 404 behavior. The user confirmed that current Cyprus and Greece-focused Hebrew articles do not make sense and should be withdrawn pending future URL-specific Hebrew content.
- [x] Remove the staged Hebrew `/he/blog/` index and all currently exposed Hebrew article routes, including Cyprus, Greece, and Cyprus-framed Wix content, after the approved decision to create future Hebrew articles separately for their appropriate URLs. English and Greek blogs are unchanged. `/he/blog/` and removed Hebrew article URLs return localized real 404s, while English and Greek article checks return 200. The canonical prerender baseline is restored to 88 routes with 11 previews and the root 404 artifact.
- [ ] After the user completes Hebrew preview QA and explicitly approves indexing, complete the final Hebrew SEO release pass: remove staging noindex, add the 19 completed Hebrew core and service-detail URLs to `sitemap.xml`, include reciprocal en/el/he/he-IL alternates only where all real translations exist, retain no Hebrew alternates for English and Greek blog routes, and validate raw indexable metadata, 88-route prerender output, real 404s, and sitemap delivery.
- [x] Verify the complete Hebrew rollout is available for user QA on the latest GitHub/Vercel `preview` deployment, while retaining staged `noindex, follow` metadata and keeping the 19 Hebrew URLs out of the public sitemap until the user explicitly approves indexing. Vercel marked commit `9d347396050211dc5dad2788a2ae85b5a71ac099` READY on the stable preview alias; all 19 completed Hebrew URLs returned 200, `/he/blog/` returned a genuine 404, and English plus Greek blog indexes returned 200. Raw deployed Hebrew metadata confirms self-canonical URLs with EN/EL/HE/he-IL alternates and staged noindex.
- [x] Freeze all Hebrew typography and visual-font changes until the user provides an approved Hebrew font system; do not attempt a typography redesign in this QA pass. This temporary constraint was superseded once the user approved Google Sans, Heebo, and Huninn for the Hebrew system; the implementation is recorded below and remains preview-only.
- [x] Audit and correct the shared language selector so exactly one language is visibly active, the current English state is never simultaneously highlighted on Hebrew, transitions between English, Greek, and Hebrew remain intentional and non-jarring, and direct language switching always reaches a real destination. Initial source QA found that English active styling used `!isGreek`, which incorrectly included Hebrew; the supplied screenshot confirmed the resulting double highlight. The control now uses an exclusive `isEnglish` state and preserves scroll position only between genuine translations.
- [x] Change English and Greek blog-to-Hebrew switching to the existing Hebrew homepage (`/he/`) because no Hebrew blog route currently exists; preserve real 404 responses for `/he/blog/` and all absent Hebrew article URLs. Regression coverage confirms both English and Greek blog fallbacks resolve to `/he/`, while the built server returns a real 404 for `/he/blog/`.
- [x] Audit the Hebrew Contact page and form against the English and Greek implementations, correct verified visual/functional parity defects without typography redesign, and test validation, submission state, direct contact actions, and RTL/LTR field behavior. The Hebrew form now uses the same form/card/field/action class hooks, accessible IDs and labels, type=email validation, scoped LTR email behavior, disabled sending state, endpoint contract, contextual error response, and mobile layout treatment as the English page. A live external submission was not sent to avoid generating an unsolicited enquiry email.
- [x] Apply the approved Hebrew typography system across the completed Hebrew rollout on `preview` only: Google Sans for body/interface text, Heebo in a deliberately broad heavy-weight hierarchy for titles and headings, and Huninn for limited warm accent treatment. English and Greek typography, canonical metadata, sitemap staging policy, and current navigation/404 behavior are preserved.
- [x] Audit and refine the completed Hebrew marketing copy for a warmer, friendlier, professional Israeli tone without emojis, artificial buzzwords, or em dashes. Refined the homepage, services, process, pricing, FAQ, templates, custom-design, mobile-first, and maps messaging. Preserved material service, pricing, legal, SEO, and form commitments, and left legal terms/policies unchanged where wording is contractual.
- [x] Verify the approved Hebrew typography and revoiced copy across all 19 completed routes at desktop and mobile breakpoints, including hierarchy, readable weights, RTL/LTR mixed-content handling, headings, labels, cards, templates, legal pages, and service details. Mobile QA found and corrected unnatural headline breaks for `Mobile-First` and `Google Maps` by using natural Hebrew display headings while preserving technical SEO terms where required. Full tests, TypeScript, production build, 88 canonical prerenders, and 11 preview prerenders all pass.
- [x] Increase the Hebrew heavy-weight design treatment across display headings, section headings, package labels, and all price/numeric treatments, while preserving readable body text and English/Greek typography isolation. Hebrew headings and price/numeric treatments now use Heebo at weight 900 with carefully scoped heavy optical reinforcement, while body text remains at its readable default.
- [x] Align all Hebrew package prices and custom-scope numeric values to the logical left edge of their RTL cards, retaining explicit LTR readability without detaching values from their plan context. All applicable values now have left-aligned isolated numeric treatment, and the Enterprise blocks on the Home and Services pages move to the logical left of their desktop RTL panels.
- [x] Refine the Hebrew cookie-consent interface: center its layout, provide comfortable spacing around all controls, replace "אישור הכל" with a natural affirmative label, replace "דחייה" with a more authentic Hebrew refusal label, and retain clear Manage and policy actions. The Hebrew dialog is horizontally centered on desktop, remains safely inset on mobile, uses "אני מאשר/ת", "לא, תודה", and "הגדרות", centers all actions, and now links to Hebrew policy routes.
- [x] Perform desktop and mobile QA for the strengthened Hebrew hierarchy, RTL price alignment, and cookie-consent changes across core pages, pricing cards, Enterprise/custom panels, and service detail routes. Visual checks at 1280 px and 390 px confirm the heavier hierarchy and centered cookie control remain contained. Regression, TypeScript, production build, 88 canonical prerenders, and 11 preview prerenders pass.
- [x] Repair the Hebrew typography regression shown in QA: remove the unsafe heavyweight treatment causing price text to overflow across pricing cards and prevent all display, heading, English-label, and price text from bleeding through containers, overlapping content, or hiding adjacent text. Replaced text-stroke inflation with contained Heebo weight hierarchy and explicit mobile wrap safeguards.
- [x] Rebuild Hebrew price typography to match the approved brand system across all pricing cards: use a contained, card-scale editorial weight for English package names and values, preserve legible one-line values where possible, and provide responsive scaling without overflow or clipping. Rebuilt `/he/pricing/` values as separate price/unit rows and scoped their responsive sizing to the card width.
- [x] Make the shared sticky header fully opaque wherever it overlays page content, preserving contrast and readable navigation on light and dark sections in Hebrew, English, and Greek routes. Both the base header and its scroll-state glass rule now use opaque `#f6f6f4` backgrounds.
- [x] Return the Hebrew cookie dialog to a compact side placement with safe desktop/mobile insets, preserving the refined Hebrew labels and avoiding excessive viewport coverage. The dialog is side-aligned, capped at 16rem, retains its natural Hebrew labels and local policy links, and keeps all consent controls reachable.
- [x] Conduct slow, page-by-page visual QA across the completed Hebrew routes on desktop and mobile, recording and correcting layout, overflow, container, contrast, and mixed-language typography defects before the repaired preview is shared. Full-page desktop and 390 px mobile checks cover every completed Hebrew core and service-detail route. Pricing values stay within their cards, hero headings remain contained after a mobile FAQ wrap correction, the sticky header retains contrast, and the compact consent dialog stays safely inset. The final suite passed 112 tests, TypeScript, and a production build with 88 canonical plus 11 preview prerenders.
- [x] Restore Hebrew pages to the exact layout, component structures, visual effects, colors, spacing, and responsive behavior of their corresponding English/Greek sources. Hebrew differs only in approved font families, natural Hebrew copy, and RTL direction. The shared Hebrew font-variable layer was applied without modifications to English or Greek page structures; the isolated pricing component was restored to the verified English editorial card, custom-panel, care-plan, and comparison-table structure.
- [x] Replace page-specific Hebrew display overrides that conflict with the approved shared font system, so body/interface text uses Google Sans and headings/price emphasis use contained Heebo without the English/Greek display fonts leaking into Hebrew layouts. Scoped Hebrew overrides now map the editorial, services, process, pricing, templates, FAQ, and contact variables to the approved font roles only under `html[dir="rtl"]`.
- [x] Rebuild the Hebrew pricing comparison section to match the English/Greek comparison component structure, visual hierarchy, typography, spacing, and responsive behavior exactly, while keeping its Hebrew content and RTL table direction. The Hebrew comparison now includes the same border/header hierarchy, plan color treatments, featured-column tint, alternating rows, status cells, and overflow wrapper as the English editorial source.
- [x] Diagnose why the cookie banner is not visible in the user’s current browser and correct only a real delivery/visibility defect. No delivery defect was found: the banner intentionally uses one language-agnostic `dm_cookie_consent` localStorage record and does not reappear once a choice is saved. Fresh preview sessions consistently show the Hebrew compact side dialog. Clear the site’s local storage or use a private window to see it again during QA; no fake repeat prompt was introduced.
- [x] Run route-by-route desktop/mobile visual QA for all completed Hebrew pages and a regression comparison for representative English and Greek pages, confirming no English or Greek changes were introduced by Hebrew typography/parity work. Desktop and 390 px checks cover all completed Hebrew core and service routes, plus English/Greek home, services, and pricing comparisons. The final validation passed 113 tests, TypeScript, and production prerendering of 88 canonical plus 11 preview routes.
- [x] Use the approved Hebrew homepage as the font and weight reference for every completed Hebrew page and section. Hebrew page-specific h1/h2/h3 selectors now enforce the homepage’s sharp Heebo hierarchy, while all eyebrow, body, label, English-fragment, and price-unit selectors resolve to Google Sans. English and Greek font files, selectors, layouts, and content are unchanged.
- [x] Inspect and correct the Israel flag artifact in the Hebrew language selector so the official flag renders cleanly without an X-like mark or broken image state. The flag now uses two separately stroked, rounded triangles to render a clean Star of David at the selector’s small size.
- [x] Defer the Services-page pricing/content restructuring requested in QA. No Services content was removed, added, or reorganized in this typography/flag pass.
- [x] Conduct page-by-page desktop and mobile typography QA against the Hebrew homepage reference, with representative English and Greek regression checks, before sharing the focused preview update. Checked Hebrew home, pricing, process, FAQ, templates, and representative English/Greek home and pricing views at 1280 px and 390 px; full test suite, TypeScript, and the 88 canonical plus 11 preview prerender build pass.
- [x] Perform a forensic route-and-section inventory of all effective Hebrew font declarations and font-related CSS overrides across the 19 completed Hebrew routes, including inline classes, page-system variables, component styles, inherited declarations, price/English fragments, form elements, accordions, cards, legal content, navigation, footer, and cookie controls. The rendered browser audit found remaining Commissioner, DM Mono, and Anybody declarations in Services, Process, Pricing, Contact, FAQ, and cookie-policy sections despite source-level variable overrides.
- [x] Replace every remaining non-approved effective Hebrew font with Google Sans for body/interface text and Heebo for headings/numeric emphasis, using RTL-scoped selectors only. Do not change English or Greek source, CSS behavior, content, components, or layouts. A strict `html[dir="rtl"] .hebrew-home` enforcement layer now resolves actual Hebrew page UI to the approved roles and includes technical monospace content. Intentional Georgia, Impact, and Nunito inside static Templates preview mockups remain untouched because they demonstrate the distinct identities of the example sites, not the DM-Labs Hebrew interface.
- [x] Verify font-family and weight consistency section by section across every completed Hebrew route at desktop and mobile breakpoints, with rendered English/Greek regression checks proving the remediation did not leak outside Hebrew. A computed-style audit reports no remaining non-approved font in 18 Hebrew routes or actual Templates page UI; only deliberately exempt sample-site mockup typography remains. Full-page mobile checks cover every Hebrew route, while English/Greek home and pricing regression checks remain visually unchanged. Full suite, TypeScript, and 88 canonical plus 11 preview prerenders pass.
- [x] Measure the current Hebrew homepage hero and its section typography at desktop and mobile to create a precise reference sheet for font family, weights, font sizes, line heights, letter spacing, eyebrow text, body text, buttons, and price rows. The measured reference is Google Sans for UI/body and Heebo for headings/numbers, with mobile/desktop hero 42.9/64 px, section 30/36 px, card heading 18 px, lead 18 px, body 14 px, eyebrow 14 px, and primary buttons 15/16 px.
- [x] Replace the current broad Hebrew heading enforcement with a measured Hebrew-only token system derived from the homepage, so every other Hebrew section matches the homepage’s visual scale rather than receiving oversized or overly heavy generic treatments. The final RTL-only token layer replaces older page-specific clamps, 900-weight price rules, 0.92 line heights, and custom display sizes with the measured homepage scales.
- [x] Audit and align every completed Hebrew page section by section to the homepage token system, including hero, section header, card, form, FAQ, pricing, legal, footer, navigation, and cookie elements. English and Greek remain strictly untouched. The implementation maps all custom Process, Pricing, Contact, FAQ, Templates, and service-detail roles to the reference system; intentionally distinct sample-site typography remains inside Templates mockups only.
- [x] Verify desktop and mobile visual consistency against the Hebrew homepage reference across all completed Hebrew routes, then run English/Greek regression checks and the full production build. Fresh 1280 px and 390 px section reviews cover all 19 Hebrew routes, and mobile English/Greek Home plus Pricing remain visually unchanged. The final validation passed 114 tests, TypeScript, and production prerendering of 88 canonical plus 11 preview routes.
- [x] Audit and correct Hebrew mobile image presentation so source images are not visibly cropped or cut off; preserve English and Greek image behavior. The Hebrew hero poster/video now use portrait-safe `object-fit: contain`, while Hebrew homepage example cards use a contained 4:5 media frame. English and Greek retain their existing cover behavior.
- [x] Reduce the Hebrew cookie banner’s mobile footprint so it does not take over the hero, while preserving consent functionality, safe insets, and the English/Greek banner presentations. The fresh Hebrew mobile banner is reduced to 13.5rem with 10–12 px mobile type, 32 px controls, 12 px side/bottom insets, and a compact title; English and Greek retain their original sizing.
- [x] Audit and improve the Hebrew mobile hero scrub experience for smooth, efficient, shorter scroll progression without changing English or Greek hero behavior. Hebrew mobile now has a 300–420 px scroll runway and a faster bounded progress controller, retaining the existing one-seek-at-a-time logic and final-frame release. Default mobile and desktop controller behavior remain unchanged for English and Greek.
- [x] Replace the malformed Hebrew language-selector flag geometry with a clear, recognizable Star of David and verify it at desktop and mobile sizes. The flag uses a single crisp two-triangle path with a reinforced 2.4 px stroke within the official blue-striped field.
- [x] Run focused Hebrew mobile and desktop QA for image framing, banner placement, hero scrub, and flag rendering, then confirm English and Greek regression isolation. Fresh 390 px hero and full-page homepage reviews confirm full-frame hero media and a compact consent card; the validation suite passed 116 tests, TypeScript, and 88 canonical plus 11 preview prerenders. English and Greek remain protected by Hebrew-only scopes and default component variants.
- [x] Inspect the **deployed** `preview` Hebrew mobile homepage at the exact reported viewport and reproduce the three unresolved issues: cropped media, oversized consent banner, and ineffective/glitchy scrub, as well as the still-incorrect Israeli flag rendering. Direct Vercel inspection confirmed the prior code was active, not cached: the hero/video used `contain`, the consent dialog was 216×207 px, and the flag contained one geometric path. The actual visual outcome remained unacceptable because `contain` introduced excessive unused hero space and the dialog overlaid too much of the hero; the scrub required an interaction-controller correction rather than source-only validation.
- [x] Replace any ineffective Hebrew mobile implementation with fixes that are demonstrably active on the deployed preview, and prove by direct deployed inspection that English and Greek are unchanged. Direct 390×844 inspection of Vercel commit `abb4756` confirms the entry hero has no consent dialog, hero video/poster use `object-fit: cover`, all example images use contained 4:5 media frames, and the Hebrew flag has one filled Star of David path. The final scrub controller remains Hebrew-specific, while CookieBanner defaults, English/Greek HomeHeroScrub variants, and non-RTL styles remain unchanged; full regression, TypeScript, and 88 canonical plus 11 preview prerenders passed.
- [x] Correct the Hebrew mobile portrait testimonial/example-card media shown in the user’s screenshot so the full portrait subject is visible in a card with a deliberate portrait aspect ratio, without changing English or Greek card framing. The Hebrew Anastacia/Tom team cards now use a dedicated 3:4 portrait frame with `object-contain` and top alignment on mobile, while desktop retains its existing cover treatment and English/Greek sources are unchanged.
- [x] Diagnose and correct the real Hebrew cookie-consent visibility flow in the user’s mobile browser, preserving consent persistence and avoiding a hero overlay while providing a clear, testable post-hero appearance path. The Hebrew banner is now absent on entry, observes the final Canvas release state, and appears after a 650ms post-release delay; a fresh 390 px mobile diagnostic confirms it is hidden through the Hero and becomes visible only after the release.
- [x] Replace the current Hebrew mobile video controller with a smoother bounded interpolation/seek strategy that visibly progresses during continuous scrolling, reverses smoothly, and completes in a shorter non-fatiguing runway without changing English or Greek scrub behavior. This video-seek approach was superseded by the approved Canvas renderer after direct Vercel diagnostics confirmed that remote mobile seeking remained unreliable despite all-intra H.264 encoding and range support.
- [x] Replace the Hebrew mobile video seek implementation with a Canvas image-sequence renderer. The single 539KB public WebP sprite contains 61 optimized 540×960 frames; Canvas renders the current scroll frame synchronously in either direction, releases at the true 78% mobile scroll end, preserves the Hebrew desktop video scrub, and does not alter English or Greek.
- [x] Verify these Hebrew-only repairs on the new Vercel preview at an Android-like mobile viewport using the exact screenshot target card, the cookie flow after Canvas release, and slow forward/reverse Canvas interactions; then confirm English and Greek remain unchanged. Direct 390×844 Vercel Canvas diagnostics confirm the public sprite loads, the drawn pixels change from entry to forward scroll and change again on reverse scroll, final release reaches `heroProgress=1`, and consent remains hidden on entry/through scrubbing before appearing 650ms after release. Portrait framing and Canvas code remain Hebrew-only; English and Greek continue using their existing video scrub and card paths.
- [x] Extend only the Hebrew mobile Canvas runway so a normal forward or reverse swipe sustains the sequence for approximately 1.5–2 seconds, without changing the Canvas asset, portrait framing, Hebrew desktop, English, or Greek behavior. The Hebrew runway is now 880–1040px, delivering approximately 600px of progressive Canvas movement before release. A controlled 1.5-second 620px forward swipe reaches the final frame, and the mirrored reverse swipe returns to the first frame. The consent dialog hides again if the user reverses back into the Hero.
- [x] Integrate Hebrew mobile hero text with the Canvas sequence: fade the text in over the final frames, hold the final frame and readable text inside the same pinned Hero for a deliberate beat, then release to the following section. Reverse scroll retracts this choreography naturally. English, Greek, and desktop remain unchanged. Direct Vercel 390px choreography checks confirm no text at 46% progress, 64% text fade over the final Canvas frames, and a fully readable held Hero at 80% progress without premature release. Canvas completes at 68% and releases only at 88%.
- [x] Keep the Hebrew mobile hero copy within the same Canvas Hero banner after the final-frame hold, then transition naturally to the following section only after a distinct final Hero exit range. The exit position is now aligned with the fixed stage, removing the observed jump where copy was visually carried into the section below. Forward and reverse 390px checks confirm the stage stays at the Hero boundary through the hold and re-enters it naturally on reverse scroll. The approved Canvas timing, English, Greek, and desktop behavior remain unchanged. All 114 tests and the production build with 88 canonical plus 11 preview prerenders pass.
- [x] Remove the Hebrew mobile Canvas/video scroll-scrub entirely and restore a fast, normal homepage entry at `/he/` on mobile. The Hero copy is now immediately visible, the mobile video has no matching source, and the Canvas returns before loading its sprite. The Hebrew desktop Hero and all English/Greek scrub behavior remain unchanged. Direct 390px mobile checks show a normal Hero entry with no pinned scroll runway. All 114 tests and the production build with 88 canonical plus 11 preview prerenders pass. The authorized main promotion is now ready.
- [x] Ship the complete Hebrew rollout to production and disable Hero video loading and scroll-scrub on mobile for English, Greek, and Hebrew homepages. Every mobile homepage now enters normally with its Hero copy immediately visible; desktop Hero behavior is preserved. Visual QA passed at 390px and desktop for all three locales; recent browser-console logs are clean; 113 tests pass; and the production build prerenders 88 canonical routes, 11 preview routes, and a root 404 artifact without errors. The release is pushed to GitHub `main` at `1d77d15246772948b34e00fd8f7930d9eda3014f`; Vercel production deployment `dpl_DrGAjSRS7wEpKTo3mm6MhtPUpcia` is READY and production English, Greek, and Hebrew homepage routes return content.
- [x] Confirm merge-readiness for `fix/mobile-hero-poster-readability` without pushing to it. Head `b4f887d8ff21f7d5fefbdddc7349ef9b1b27196a` descends directly from GitHub main `a80fc5d`; the diff is one CSS file only with 29 additions and 1 deletion, `git diff --check` is clean, no pull request or review threads exist, and GitHub reports no Actions runs for that branch. No regression was found in the branch scope.
- [x] Replace the mobile-only three-flag language control with one current-locale chip that opens the existing Radix sheet as a bottom sheet listing EN, EL, and HE. Desktop retains the original three-flag row; existing language-routing, hreflang, fallback, LTR, and RTL logic remains in use. Throttled 390px production-output browser checks confirm EN, EL, and HE choices, navigation to Greek, and RTL direction in the Hebrew sheet.
- [x] Investigate and correct the initial homepage flash on throttled mobile loading. The client now hydrates static prerender output when the root already contains route markup and retains a safe createRoot mount only for an empty development root. A throttled production-output browser check confirmed static root content and visible Hero copy without hydration warnings. The only observed local 404 requests were the expected absent Vercel Insights script from the local Node server, unrelated to the entry transition.
- [x] Conduct thorough preview-only functional QA across Hebrew navigation, language switching, canonical/hreflang metadata, static-route behavior, and real 404 delivery; report separate confidence percentages for visual presentation pending typography, functional behavior, navigation, multilingual SEO, and 404 integrity. The prior QA covered all 19 completed Hebrew routes, direct language fallbacks, staged canonical/noindex metadata, English/Greek blog exclusions, and real Hebrew 404 behavior. The typography follow-up further passed 106 full tests, TypeScript, 88 canonical prerenders, 11 preview prerenders, and desktop/mobile visual checks. Sitemap work remains deferred until after user QA and explicit indexing approval.
